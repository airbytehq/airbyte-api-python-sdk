"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import date
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class MercadoAds(str, Enum):
    MERCADO_ADS = "mercado-ads"


class SourceMercadoAdsTypedDict(TypedDict):
    client_id: str
    client_refresh_token: str
    client_secret: str
    end_date: NotRequired[date]
    r"""Cannot exceed 90 days from current day for Product Ads"""
    lookback_days: NotRequired[float]
    source_type: MercadoAds
    start_date: NotRequired[date]
    r"""Cannot exceed 90 days from current day for Product Ads, and 90 days from \"End Date\" on Brand and Display Ads"""


class SourceMercadoAds(BaseModel):
    client_id: str

    client_refresh_token: str

    client_secret: str

    end_date: Optional[date] = None
    r"""Cannot exceed 90 days from current day for Product Ads"""

    lookback_days: Optional[float] = 7

    SOURCE_TYPE: Annotated[
        Annotated[MercadoAds, AfterValidator(validate_const(MercadoAds.MERCADO_ADS))],
        pydantic.Field(alias="sourceType"),
    ] = MercadoAds.MERCADO_ADS

    start_date: Optional[date] = None
    r"""Cannot exceed 90 days from current day for Product Ads, and 90 days from \"End Date\" on Brand and Display Ads"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["end_date", "lookback_days", "start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceMercadoAds.model_rebuild()
except NameError:
    pass
