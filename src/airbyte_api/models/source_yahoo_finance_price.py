"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class SourceYahooFinancePriceInterval(str, Enum):
    r"""The interval of between prices queried."""

    ONEM = "1m"
    FIVEM = "5m"
    FIFTEENM = "15m"
    THIRTYM = "30m"
    NINETYM = "90m"
    ONEH = "1h"
    ONED = "1d"
    FIVED = "5d"
    ONEWK = "1wk"
    ONEMO = "1mo"
    THREEMO = "3mo"


class Range(str, Enum):
    r"""The range of prices to be queried."""

    ONED = "1d"
    FIVED = "5d"
    SEVEND = "7d"
    ONEMO = "1mo"
    THREEMO = "3mo"
    SIXMO = "6mo"
    ONEY = "1y"
    TWOY = "2y"
    FIVEY = "5y"
    YTD = "ytd"
    MAX = "max"


class YahooFinancePrice(str, Enum):
    YAHOO_FINANCE_PRICE = "yahoo-finance-price"


class SourceYahooFinancePriceTypedDict(TypedDict):
    tickers: str
    r"""Comma-separated identifiers for the stocks to be queried. Whitespaces are allowed."""
    interval: NotRequired[SourceYahooFinancePriceInterval]
    r"""The interval of between prices queried."""
    range: NotRequired[Range]
    r"""The range of prices to be queried."""
    source_type: YahooFinancePrice


class SourceYahooFinancePrice(BaseModel):
    tickers: str
    r"""Comma-separated identifiers for the stocks to be queried. Whitespaces are allowed."""

    interval: Optional[SourceYahooFinancePriceInterval] = None
    r"""The interval of between prices queried."""

    range: Optional[Range] = None
    r"""The range of prices to be queried."""

    SOURCE_TYPE: Annotated[
        Annotated[
            YahooFinancePrice,
            AfterValidator(validate_const(YahooFinancePrice.YAHOO_FINANCE_PRICE)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = YahooFinancePrice.YAHOO_FINANCE_PRICE

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval", "range"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceYahooFinancePrice.model_rebuild()
except NameError:
    pass
