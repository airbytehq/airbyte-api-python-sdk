"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Factorial(str, Enum):
    FACTORIAL = "factorial"


class SourceFactorialTypedDict(TypedDict):
    api_key: str
    start_date: datetime
    limit: NotRequired[str]
    r"""Max records per page limit"""
    source_type: Factorial


class SourceFactorial(BaseModel):
    api_key: str

    start_date: datetime

    limit: Optional[str] = "50"
    r"""Max records per page limit"""

    SOURCE_TYPE: Annotated[
        Annotated[Factorial, AfterValidator(validate_const(Factorial.FACTORIAL))],
        pydantic.Field(alias="sourceType"),
    ] = Factorial.FACTORIAL

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["limit"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceFactorial.model_rebuild()
except NameError:
    pass
