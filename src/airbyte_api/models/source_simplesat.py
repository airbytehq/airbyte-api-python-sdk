"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Simplesat(str, Enum):
    SIMPLESAT = "simplesat"


class SourceSimplesatTypedDict(TypedDict):
    api_key: str
    end_date: NotRequired[datetime]
    r"""Date till when the sync should end"""
    source_type: Simplesat
    start_date: NotRequired[datetime]
    r"""Date from when the sync should start"""


class SourceSimplesat(BaseModel):
    api_key: str

    end_date: Optional[datetime] = None
    r"""Date till when the sync should end"""

    SOURCE_TYPE: Annotated[
        Annotated[Simplesat, AfterValidator(validate_const(Simplesat.SIMPLESAT))],
        pydantic.Field(alias="sourceType"),
    ] = Simplesat.SIMPLESAT

    start_date: Optional[datetime] = None
    r"""Date from when the sync should start"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["end_date", "start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceSimplesat.model_rebuild()
except NameError:
    pass
