"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import date
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Days(str, Enum):
    r"""The number of days of data for market chart."""

    ONE = "1"
    SEVEN = "7"
    FOURTEEN = "14"
    THIRTY = "30"
    NINETY = "90"
    ONE_HUNDRED_AND_EIGHTY = "180"
    THREE_HUNDRED_AND_SIXTY_FIVE = "365"
    MAX = "max"


class CoingeckoCoins(str, Enum):
    COINGECKO_COINS = "coingecko-coins"


class SourceCoingeckoCoinsTypedDict(TypedDict):
    coin_id: str
    r"""CoinGecko coin ID (e.g. bitcoin). Can be retrieved from the
    `/coins/list` endpoint.

    """
    start_date: date
    r"""The start date for the historical data stream in dd-mm-yyyy format.

    """
    vs_currency: str
    r"""The target currency of market data (e.g. usd, eur, jpy, etc.)

    """
    api_key: NotRequired[str]
    r"""API Key (for pro users)"""
    days: NotRequired[Days]
    r"""The number of days of data for market chart.

    """
    end_date: NotRequired[date]
    r"""The end date for the historical data stream in dd-mm-yyyy format.

    """
    source_type: CoingeckoCoins


class SourceCoingeckoCoins(BaseModel):
    coin_id: str
    r"""CoinGecko coin ID (e.g. bitcoin). Can be retrieved from the
    `/coins/list` endpoint.

    """

    start_date: date
    r"""The start date for the historical data stream in dd-mm-yyyy format.

    """

    vs_currency: str
    r"""The target currency of market data (e.g. usd, eur, jpy, etc.)

    """

    api_key: Optional[str] = None
    r"""API Key (for pro users)"""

    days: Optional[Days] = Days.THIRTY
    r"""The number of days of data for market chart.

    """

    end_date: Optional[date] = None
    r"""The end date for the historical data stream in dd-mm-yyyy format.

    """

    SOURCE_TYPE: Annotated[
        Annotated[
            CoingeckoCoins,
            AfterValidator(validate_const(CoingeckoCoins.COINGECKO_COINS)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = CoingeckoCoins.COINGECKO_COINS

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["api_key", "days", "end_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceCoingeckoCoins.model_rebuild()
except NameError:
    pass
