"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Opsgenie(str, Enum):
    OPSGENIE = "opsgenie"


class SourceOpsgenieTypedDict(TypedDict):
    api_token: str
    r"""API token used to access the Opsgenie platform"""
    endpoint: NotRequired[str]
    r"""Service endpoint to use for API calls."""
    source_type: Opsgenie
    start_date: NotRequired[str]
    r"""The date from which you'd like to replicate data from Opsgenie in the format of YYYY-MM-DDT00:00:00Z. All data generated after this date will be replicated. Note that it will be used only in the following incremental streams: issues."""


class SourceOpsgenie(BaseModel):
    api_token: str
    r"""API token used to access the Opsgenie platform"""

    endpoint: Optional[str] = "api.opsgenie.com"
    r"""Service endpoint to use for API calls."""

    SOURCE_TYPE: Annotated[
        Annotated[Opsgenie, AfterValidator(validate_const(Opsgenie.OPSGENIE))],
        pydantic.Field(alias="sourceType"),
    ] = Opsgenie.OPSGENIE

    start_date: Optional[str] = None
    r"""The date from which you'd like to replicate data from Opsgenie in the format of YYYY-MM-DDT00:00:00Z. All data generated after this date will be replicated. Note that it will be used only in the following incremental streams: issues."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["endpoint", "start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceOpsgenie.model_rebuild()
except NameError:
    pass
