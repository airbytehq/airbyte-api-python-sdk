"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Kyve(str, Enum):
    KYVE = "kyve"


class SourceKyveTypedDict(TypedDict):
    pool_ids: str
    r"""The IDs of the KYVE storage pool you want to archive. (Comma separated)"""
    start_ids: str
    r"""The start-id defines, from which bundle id the pipeline should start to extract the data. (Comma separated)"""
    source_type: Kyve
    url_base: NotRequired[str]
    r"""URL to the KYVE Chain API."""


class SourceKyve(BaseModel):
    pool_ids: str
    r"""The IDs of the KYVE storage pool you want to archive. (Comma separated)"""

    start_ids: str
    r"""The start-id defines, from which bundle id the pipeline should start to extract the data. (Comma separated)"""

    SOURCE_TYPE: Annotated[
        Annotated[Kyve, AfterValidator(validate_const(Kyve.KYVE))],
        pydantic.Field(alias="sourceType"),
    ] = Kyve.KYVE

    url_base: Optional[str] = "https://api.kyve.network"
    r"""URL to the KYVE Chain API."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["url_base"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceKyve.model_rebuild()
except NameError:
    pass
