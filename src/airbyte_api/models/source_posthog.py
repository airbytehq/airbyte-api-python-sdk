"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class Posthog(str, Enum):
    POSTHOG = "posthog"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourcePosthogTypedDict(TypedDict):
    api_key: str
    r"""API Key. See the <a href=\"https://docs.airbyte.com/integrations/sources/posthog\">docs</a> for information on how to generate this key."""
    start_date: datetime
    r"""The date from which you'd like to replicate the data. Any data before this date will not be replicated."""
    base_url: NotRequired[str]
    r"""Base PostHog url. Defaults to PostHog Cloud (https://app.posthog.com)."""
    events_time_step: NotRequired[int]
    r"""Set lower value in case of failing long running sync of events stream."""
    source_type: Posthog


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourcePosthog(BaseModel):
    api_key: str
    r"""API Key. See the <a href=\"https://docs.airbyte.com/integrations/sources/posthog\">docs</a> for information on how to generate this key."""

    start_date: datetime
    r"""The date from which you'd like to replicate the data. Any data before this date will not be replicated."""

    base_url: Optional[str] = "https://app.posthog.com"
    r"""Base PostHog url. Defaults to PostHog Cloud (https://app.posthog.com)."""

    events_time_step: Optional[int] = 30
    r"""Set lower value in case of failing long running sync of events stream."""

    SOURCE_TYPE: Annotated[
        Annotated[Posthog, AfterValidator(validate_const(Posthog.POSTHOG))],
        pydantic.Field(alias="sourceType"),
    ] = Posthog.POSTHOG

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["base_url", "events_time_step"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourcePosthog.model_rebuild()
except NameError:
    pass
