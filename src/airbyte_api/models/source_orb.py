"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class Orb(str, Enum):
    ORB = "orb"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceOrbTypedDict(TypedDict):
    api_key: str
    r"""Orb API Key, issued from the Orb admin console."""
    start_date: datetime
    r"""UTC date and time in the format 2022-03-01T00:00:00Z. Any data with created_at before this data will not be synced. For Subscription Usage, this becomes the `timeframe_start` API parameter."""
    end_date: NotRequired[str]
    r"""UTC date and time in the format 2022-03-01T00:00:00Z. Any data with created_at after this data will not be synced. For Subscription Usage, this becomes the `timeframe_start` API parameter."""
    lookback_window_days: NotRequired[int]
    r"""When set to N, the connector will always refresh resources created within the past N days. By default, updated objects that are not newly created are not incrementally synced."""
    numeric_event_properties_keys: NotRequired[List[str]]
    r"""Property key names to extract from all events, in order to enrich ledger entries corresponding to an event deduction."""
    plan_id: NotRequired[str]
    r"""Orb Plan ID to filter subscriptions that should have usage fetched."""
    source_type: Orb
    string_event_properties_keys: NotRequired[List[str]]
    r"""Property key names to extract from all events, in order to enrich ledger entries corresponding to an event deduction."""
    subscription_usage_grouping_key: NotRequired[str]
    r"""Property key name to group subscription usage by."""


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceOrb(BaseModel):
    api_key: str
    r"""Orb API Key, issued from the Orb admin console."""

    start_date: datetime
    r"""UTC date and time in the format 2022-03-01T00:00:00Z. Any data with created_at before this data will not be synced. For Subscription Usage, this becomes the `timeframe_start` API parameter."""

    end_date: Optional[str] = None
    r"""UTC date and time in the format 2022-03-01T00:00:00Z. Any data with created_at after this data will not be synced. For Subscription Usage, this becomes the `timeframe_start` API parameter."""

    lookback_window_days: Optional[int] = 0
    r"""When set to N, the connector will always refresh resources created within the past N days. By default, updated objects that are not newly created are not incrementally synced."""

    numeric_event_properties_keys: Optional[List[str]] = None
    r"""Property key names to extract from all events, in order to enrich ledger entries corresponding to an event deduction."""

    plan_id: Optional[str] = None
    r"""Orb Plan ID to filter subscriptions that should have usage fetched."""

    SOURCE_TYPE: Annotated[
        Annotated[Orb, AfterValidator(validate_const(Orb.ORB))],
        pydantic.Field(alias="sourceType"),
    ] = Orb.ORB

    string_event_properties_keys: Optional[List[str]] = None
    r"""Property key names to extract from all events, in order to enrich ledger entries corresponding to an event deduction."""

    subscription_usage_grouping_key: Optional[str] = None
    r"""Property key name to group subscription usage by."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "end_date",
                "lookback_window_days",
                "numeric_event_properties_keys",
                "plan_id",
                "string_event_properties_keys",
                "subscription_usage_grouping_key",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceOrb.model_rebuild()
except NameError:
    pass
