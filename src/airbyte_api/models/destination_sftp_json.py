"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class SftpJSON(str, Enum):
    SFTP_JSON = "sftp-json"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'destinationType' key instead.."
)
class DestinationSftpJSONTypedDict(TypedDict):
    destination_path: str
    r"""Path to the directory where json files will be written."""
    host: str
    r"""Hostname of the SFTP server."""
    password: str
    r"""Password associated with the username."""
    username: str
    r"""Username to use to access the SFTP server."""
    destination_type: SftpJSON
    port: NotRequired[int]
    r"""Port of the SFTP server."""


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'destinationType' key instead.."
)
class DestinationSftpJSON(BaseModel):
    destination_path: str
    r"""Path to the directory where json files will be written."""

    host: str
    r"""Hostname of the SFTP server."""

    password: str
    r"""Password associated with the username."""

    username: str
    r"""Username to use to access the SFTP server."""

    DESTINATION_TYPE: Annotated[
        Annotated[SftpJSON, AfterValidator(validate_const(SftpJSON.SFTP_JSON))],
        pydantic.Field(alias="destinationType"),
    ] = SftpJSON.SFTP_JSON

    port: Optional[int] = 22
    r"""Port of the SFTP server."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["port"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    DestinationSftpJSON.model_rebuild()
except NameError:
    pass
