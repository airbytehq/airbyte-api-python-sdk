"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel
import pydantic
from pydantic import ConfigDict
from typing import Any, Dict
from typing_extensions import Annotated, TypedDict


class SourceConfigurationTypedDict(TypedDict):
    r"""The values required to configure the source. Must include a `sourceType` string identifying the connector (e.g. 'postgres', 'github'). Additional properties depend on the connector type; the API validates the full schema server-side."""

    source_type: str
    r"""The type of source connector."""


class SourceConfiguration(BaseModel):
    r"""The values required to configure the source. Must include a `sourceType` string identifying the connector (e.g. 'postgres', 'github'). Additional properties depend on the connector type; the API validates the full schema server-side."""

    model_config = ConfigDict(
        populate_by_name=True, arbitrary_types_allowed=True, extra="allow"
    )
    __pydantic_extra__: Dict[str, Any] = pydantic.Field(init=False)

    source_type: Annotated[str, pydantic.Field(alias="sourceType")]
    r"""The type of source connector."""

    @property
    def additional_properties(self):
        return self.__pydantic_extra__

    @additional_properties.setter
    def additional_properties(self, value):
        self.__pydantic_extra__ = value  # pyright: ignore[reportIncompatibleVariableOverride]


try:
    SourceConfiguration.model_rebuild()
except NameError:
    pass
