"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Height(str, Enum):
    HEIGHT = "height"


class SourceHeightTypedDict(TypedDict):
    api_key: str
    start_date: datetime
    search_query: NotRequired[str]
    r"""Search query to be used with search stream"""
    source_type: Height


class SourceHeight(BaseModel):
    api_key: str

    start_date: datetime

    search_query: Optional[str] = "task"
    r"""Search query to be used with search stream"""

    SOURCE_TYPE: Annotated[
        Annotated[Height, AfterValidator(validate_const(Height.HEIGHT))],
        pydantic.Field(alias="sourceType"),
    ] = Height.HEIGHT

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["search_query"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceHeight.model_rebuild()
except NameError:
    pass
