"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Imagga(str, Enum):
    IMAGGA = "imagga"


class SourceImaggaTypedDict(TypedDict):
    api_key: str
    r"""Your Imagga API key, available in your Imagga dashboard. Could be found at `https://imagga.com/profile/dashboard`"""
    api_secret: str
    r"""Your Imagga API secret, available in your Imagga dashboard. Could be found at `https://imagga.com/profile/dashboard`"""
    img_for_detection: NotRequired[str]
    r"""An image for detection endpoints"""
    source_type: Imagga


class SourceImagga(BaseModel):
    api_key: str
    r"""Your Imagga API key, available in your Imagga dashboard. Could be found at `https://imagga.com/profile/dashboard`"""

    api_secret: str
    r"""Your Imagga API secret, available in your Imagga dashboard. Could be found at `https://imagga.com/profile/dashboard`"""

    img_for_detection: Optional[str] = (
        "https://imagga.com/static/images/categorization/child-476506_640.jpg"
    )
    r"""An image for detection endpoints"""

    SOURCE_TYPE: Annotated[
        Annotated[Imagga, AfterValidator(validate_const(Imagga.IMAGGA))],
        pydantic.Field(alias="sourceType"),
    ] = Imagga.IMAGGA

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["img_for_detection"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceImagga.model_rebuild()
except NameError:
    pass
