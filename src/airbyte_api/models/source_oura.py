"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class Oura(str, Enum):
    OURA = "oura"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceOuraTypedDict(TypedDict):
    api_key: str
    r"""API Key"""
    end_datetime: NotRequired[datetime]
    r"""End datetime to sync until. Default is current UTC datetime."""
    source_type: Oura
    start_datetime: NotRequired[datetime]
    r"""Start datetime to sync from. Default is current UTC datetime minus 1
    day.

    """


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceOura(BaseModel):
    api_key: str
    r"""API Key"""

    end_datetime: Optional[datetime] = None
    r"""End datetime to sync until. Default is current UTC datetime."""

    SOURCE_TYPE: Annotated[
        Annotated[Oura, AfterValidator(validate_const(Oura.OURA))],
        pydantic.Field(alias="sourceType"),
    ] = Oura.OURA

    start_datetime: Optional[datetime] = None
    r"""Start datetime to sync from. Default is current UTC datetime minus 1
    day.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["end_datetime", "start_datetime"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceOura.model_rebuild()
except NameError:
    pass
