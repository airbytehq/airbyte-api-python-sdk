"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class Vitally(str, Enum):
    VITALLY = "vitally"


class SourceVitallyStatus(str, Enum):
    r"""Status of the Vitally accounts. One of the following values; active, churned, activeOrChurned."""

    ACTIVE = "active"
    CHURNED = "churned"
    ACTIVE_OR_CHURNED = "activeOrChurned"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceVitallyTypedDict(TypedDict):
    domain: str
    r"""Provide only the subdomain part, like https://{your-custom-subdomain}.rest.vitally.io/.  Keep empty if you don't have a subdomain."""
    secret_token: str
    r"""sk_live_secret_token"""
    status: SourceVitallyStatus
    r"""Status of the Vitally accounts. One of the following values; active, churned, activeOrChurned."""
    basic_auth_header: NotRequired[str]
    r"""Basic Auth Header"""
    source_type: Vitally


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceVitally(BaseModel):
    domain: str
    r"""Provide only the subdomain part, like https://{your-custom-subdomain}.rest.vitally.io/.  Keep empty if you don't have a subdomain."""

    secret_token: str
    r"""sk_live_secret_token"""

    status: SourceVitallyStatus
    r"""Status of the Vitally accounts. One of the following values; active, churned, activeOrChurned."""

    basic_auth_header: Optional[str] = None
    r"""Basic Auth Header"""

    SOURCE_TYPE: Annotated[
        Annotated[Vitally, AfterValidator(validate_const(Vitally.VITALLY))],
        pydantic.Field(alias="sourceType"),
    ] = Vitally.VITALLY

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["basic_auth_header"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceVitally.model_rebuild()
except NameError:
    pass
