"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import parse_datetime, validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Pardot(str, Enum):
    PARDOT = "pardot"


class SourcePardotTypedDict(TypedDict):
    client_id: str
    r"""The Consumer Key that can be found when viewing your app in Salesforce"""
    client_secret: str
    r"""The Consumer Secret that can be found when viewing your app in Salesforce"""
    pardot_business_unit_id: str
    r"""Pardot Business ID, can be found at Setup > Pardot > Pardot Account Setup"""
    refresh_token: str
    r"""Salesforce Refresh Token used for Airbyte to access your Salesforce account. If you don't know what this is, follow this <a href=\"https://medium.com/@bpmmendis94/obtain-access-refresh-tokens-from-salesforce-rest-api-a324fe4ccd9b\">guide</a> to retrieve it."""
    is_sandbox: NotRequired[bool]
    r"""Whether or not the the app is in a Salesforce sandbox. If you do not know what this, assume it is false."""
    source_type: Pardot
    start_date: NotRequired[datetime]
    r"""UTC date and time in the format 2000-01-01T00:00:00Z. Any data before this date will not be replicated. Defaults to the year Pardot was released."""


class SourcePardot(BaseModel):
    client_id: str
    r"""The Consumer Key that can be found when viewing your app in Salesforce"""

    client_secret: str
    r"""The Consumer Secret that can be found when viewing your app in Salesforce"""

    pardot_business_unit_id: str
    r"""Pardot Business ID, can be found at Setup > Pardot > Pardot Account Setup"""

    refresh_token: str
    r"""Salesforce Refresh Token used for Airbyte to access your Salesforce account. If you don't know what this is, follow this <a href=\"https://medium.com/@bpmmendis94/obtain-access-refresh-tokens-from-salesforce-rest-api-a324fe4ccd9b\">guide</a> to retrieve it."""

    is_sandbox: Optional[bool] = False
    r"""Whether or not the the app is in a Salesforce sandbox. If you do not know what this, assume it is false."""

    SOURCE_TYPE: Annotated[
        Annotated[Pardot, AfterValidator(validate_const(Pardot.PARDOT))],
        pydantic.Field(alias="sourceType"),
    ] = Pardot.PARDOT

    start_date: Optional[datetime] = parse_datetime("2007-01-01T00:00:00Z")
    r"""UTC date and time in the format 2000-01-01T00:00:00Z. Any data before this date will not be replicated. Defaults to the year Pardot was released."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["is_sandbox", "start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourcePardot.model_rebuild()
except NameError:
    pass
