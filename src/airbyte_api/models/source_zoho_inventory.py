"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class Domain(str, Enum):
    r"""The domain suffix for the Zoho Inventory API based on your data center location (e.g., 'com', 'eu', 'in', etc.)"""

    COM = "com"
    IN = "in"
    JP = "jp"
    EU = "eu"
    COM_AU = "com.au"
    CA = "ca"
    COM_CN = "com.cn"
    SA = "sa"


class ZohoInventory(str, Enum):
    ZOHO_INVENTORY = "zoho-inventory"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceZohoInventoryTypedDict(TypedDict):
    client_id: str
    client_secret: str
    refresh_token: str
    start_date: datetime
    domain: NotRequired[Domain]
    r"""The domain suffix for the Zoho Inventory API based on your data center location (e.g., 'com', 'eu', 'in', etc.)"""
    source_type: ZohoInventory


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceZohoInventory(BaseModel):
    client_id: str

    client_secret: str

    refresh_token: str

    start_date: datetime

    domain: Optional[Domain] = Domain.COM
    r"""The domain suffix for the Zoho Inventory API based on your data center location (e.g., 'com', 'eu', 'in', etc.)"""

    SOURCE_TYPE: Annotated[
        Annotated[
            ZohoInventory, AfterValidator(validate_const(ZohoInventory.ZOHO_INVENTORY))
        ],
        pydantic.Field(alias="sourceType"),
    ] = ZohoInventory.ZOHO_INVENTORY

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["domain"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceZohoInventory.model_rebuild()
except NameError:
    pass
