"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class Cimis(str, Enum):
    CIMIS = "cimis"


class TargetsType(str, Enum):
    WSN_STATION_NUMBERS = "WSN station numbers"
    CALIFORNIA_ZIP_CODES = "California zip codes"
    DECIMAL_DEGREE_COORDINATES = "decimal-degree coordinates"
    STREET_ADDRESSES = "street addresses"


class UnitOfMeasure(str, Enum):
    E = "E"
    M = "M"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceCimisTypedDict(TypedDict):
    api_key: str
    end_date: datetime
    start_date: datetime
    targets: List[Any]
    targets_type: TargetsType
    daily_data_items: NotRequired[List[Any]]
    hourly_data_items: NotRequired[List[Any]]
    source_type: Cimis
    unit_of_measure: NotRequired[UnitOfMeasure]


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceCimis(BaseModel):
    api_key: str

    end_date: datetime

    start_date: datetime

    targets: List[Any]

    targets_type: TargetsType

    daily_data_items: Optional[List[Any]] = None

    hourly_data_items: Optional[List[Any]] = None

    SOURCE_TYPE: Annotated[
        Annotated[Cimis, AfterValidator(validate_const(Cimis.CIMIS))],
        pydantic.Field(alias="sourceType"),
    ] = Cimis.CIMIS

    unit_of_measure: Optional[UnitOfMeasure] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["daily_data_items", "hourly_data_items", "unit_of_measure"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceCimis.model_rebuild()
except NameError:
    pass
