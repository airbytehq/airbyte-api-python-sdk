"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import date
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class PolygonStockAPI(str, Enum):
    POLYGON_STOCK_API = "polygon-stock-api"


class SourcePolygonStockAPITypedDict(TypedDict):
    api_key: str
    r"""Your API ACCESS Key"""
    end_date: date
    r"""The target date for the aggregate window."""
    multiplier: int
    r"""The size of the timespan multiplier."""
    start_date: date
    r"""The beginning date for the aggregate window."""
    stocks_ticker: str
    r"""The exchange symbol that this item is traded under."""
    timespan: str
    r"""The size of the time window."""
    adjusted: NotRequired[str]
    r"""Determines whether or not the results are adjusted for splits. By default, results are adjusted and set to true. Set this to false to get results that are NOT adjusted for splits."""
    limit: NotRequired[int]
    r"""The target date for the aggregate window."""
    sort: NotRequired[str]
    r"""Sort the results by timestamp. asc will return results in ascending order (oldest at the top), desc will return results in descending order (newest at the top)."""
    source_type: PolygonStockAPI


class SourcePolygonStockAPI(BaseModel):
    api_key: Annotated[str, pydantic.Field(alias="apiKey")]
    r"""Your API ACCESS Key"""

    end_date: date
    r"""The target date for the aggregate window."""

    multiplier: int
    r"""The size of the timespan multiplier."""

    start_date: date
    r"""The beginning date for the aggregate window."""

    stocks_ticker: Annotated[str, pydantic.Field(alias="stocksTicker")]
    r"""The exchange symbol that this item is traded under."""

    timespan: str
    r"""The size of the time window."""

    adjusted: Optional[str] = None
    r"""Determines whether or not the results are adjusted for splits. By default, results are adjusted and set to true. Set this to false to get results that are NOT adjusted for splits."""

    limit: Optional[int] = None
    r"""The target date for the aggregate window."""

    sort: Optional[str] = None
    r"""Sort the results by timestamp. asc will return results in ascending order (oldest at the top), desc will return results in descending order (newest at the top)."""

    SOURCE_TYPE: Annotated[
        Annotated[
            PolygonStockAPI,
            AfterValidator(validate_const(PolygonStockAPI.POLYGON_STOCK_API)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = PolygonStockAPI.POLYGON_STOCK_API

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["adjusted", "limit", "sort"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourcePolygonStockAPI.model_rebuild()
except NameError:
    pass
