"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class TrackPms(str, Enum):
    TRACK_PMS = "track-pms"


class SourceTrackPmsTypedDict(TypedDict):
    api_key: str
    customer_domain: str
    api_secret: NotRequired[str]
    source_type: TrackPms


class SourceTrackPms(BaseModel):
    api_key: str

    customer_domain: str

    api_secret: Optional[str] = None

    SOURCE_TYPE: Annotated[
        Annotated[TrackPms, AfterValidator(validate_const(TrackPms.TRACK_PMS))],
        pydantic.Field(alias="sourceType"),
    ] = TrackPms.TRACK_PMS

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["api_secret"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceTrackPms.model_rebuild()
except NameError:
    pass
