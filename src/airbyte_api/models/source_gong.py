"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Gong(str, Enum):
    GONG = "gong"


class SourceGongTypedDict(TypedDict):
    access_key: str
    r"""Gong Access Key"""
    access_key_secret: str
    r"""Gong Access Key Secret"""
    source_type: Gong
    start_date: NotRequired[str]
    r"""The date from which to list calls, in the ISO-8601 format; if not specified, the calls start with the earliest recorded call. For web-conference calls recorded by Gong, the date denotes its scheduled time, otherwise, it denotes its actual start time."""


class SourceGong(BaseModel):
    access_key: str
    r"""Gong Access Key"""

    access_key_secret: str
    r"""Gong Access Key Secret"""

    SOURCE_TYPE: Annotated[
        Annotated[Gong, AfterValidator(validate_const(Gong.GONG))],
        pydantic.Field(alias="sourceType"),
    ] = Gong.GONG

    start_date: Optional[str] = None
    r"""The date from which to list calls, in the ISO-8601 format; if not specified, the calls start with the earliest recorded call. For web-conference calls recorded by Gong, the date denotes its scheduled time, otherwise, it denotes its actual start time."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceGong.model_rebuild()
except NameError:
    pass
