"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import get_discriminator, validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import Discriminator, Tag, model_serializer
from pydantic.functional_validators import AfterValidator
from typing import List, Optional, Union
from typing_extensions import (
    Annotated,
    NotRequired,
    TypeAliasType,
    TypedDict,
    deprecated,
)


class OptionTitleAPITokenCredentials(str, Enum):
    API_TOKEN_CREDENTIALS = "API Token Credentials"


class SourceSlackAPITokenTypedDict(TypedDict):
    api_token: str
    r"""A Slack bot token. See the <a href=\"https://docs.airbyte.com/integrations/sources/slack\">docs</a> for instructions on how to generate it."""
    option_title: OptionTitleAPITokenCredentials


class SourceSlackAPIToken(BaseModel):
    api_token: str
    r"""A Slack bot token. See the <a href=\"https://docs.airbyte.com/integrations/sources/slack\">docs</a> for instructions on how to generate it."""

    OPTION_TITLE: Annotated[
        Annotated[
            OptionTitleAPITokenCredentials,
            AfterValidator(
                validate_const(OptionTitleAPITokenCredentials.API_TOKEN_CREDENTIALS)
            ),
        ],
        pydantic.Field(alias="option_title"),
    ] = OptionTitleAPITokenCredentials.API_TOKEN_CREDENTIALS


class OptionTitleDefaultOAuth20Authorization(str, Enum):
    DEFAULT_O_AUTH2_0_AUTHORIZATION = "Default OAuth2.0 authorization"


class SignInViaSlackOAuthTypedDict(TypedDict):
    access_token: str
    r"""Slack access_token. See our <a href=\"https://docs.airbyte.com/integrations/sources/slack\">docs</a> if you need help generating the token."""
    client_id: str
    r"""Slack client_id. See our <a href=\"https://docs.airbyte.com/integrations/sources/slack\">docs</a> if you need help finding this id."""
    client_secret: str
    r"""Slack client_secret. See our <a href=\"https://docs.airbyte.com/integrations/sources/slack\">docs</a> if you need help finding this secret."""
    option_title: OptionTitleDefaultOAuth20Authorization


class SignInViaSlackOAuth(BaseModel):
    access_token: str
    r"""Slack access_token. See our <a href=\"https://docs.airbyte.com/integrations/sources/slack\">docs</a> if you need help generating the token."""

    client_id: str
    r"""Slack client_id. See our <a href=\"https://docs.airbyte.com/integrations/sources/slack\">docs</a> if you need help finding this id."""

    client_secret: str
    r"""Slack client_secret. See our <a href=\"https://docs.airbyte.com/integrations/sources/slack\">docs</a> if you need help finding this secret."""

    OPTION_TITLE: Annotated[
        Annotated[
            OptionTitleDefaultOAuth20Authorization,
            AfterValidator(
                validate_const(
                    OptionTitleDefaultOAuth20Authorization.DEFAULT_O_AUTH2_0_AUTHORIZATION
                )
            ),
        ],
        pydantic.Field(alias="option_title"),
    ] = OptionTitleDefaultOAuth20Authorization.DEFAULT_O_AUTH2_0_AUTHORIZATION


SourceSlackAuthenticationMechanismTypedDict = TypeAliasType(
    "SourceSlackAuthenticationMechanismTypedDict",
    Union[SourceSlackAPITokenTypedDict, SignInViaSlackOAuthTypedDict],
)
r"""Choose how to authenticate into Slack"""


SourceSlackAuthenticationMechanism = Annotated[
    Union[
        Annotated[SignInViaSlackOAuth, Tag("Default OAuth2.0 authorization")],
        Annotated[SourceSlackAPIToken, Tag("API Token Credentials")],
    ],
    Discriminator(lambda m: get_discriminator(m, "option_title", "option_title")),
]
r"""Choose how to authenticate into Slack"""


class SlackEnum(str, Enum):
    SLACK = "slack"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceSlackTypedDict(TypedDict):
    start_date: datetime
    r"""UTC date and time in the format 2017-01-25T00:00:00Z. Any data before this date will not be replicated."""
    channel_filter: NotRequired[List[str]]
    r"""A channel name list (without leading '#' char) which limit the channels from which you'd like to sync. Empty list means no filter."""
    channel_messages_window_size: NotRequired[int]
    r"""The size (in days) of the date window that will be used while syncing data from the channel messages stream. A smaller window will allow for greater parallelization when syncing records, but can lead to rate limiting errors."""
    credentials: NotRequired[SourceSlackAuthenticationMechanismTypedDict]
    r"""Choose how to authenticate into Slack"""
    include_private_channels: NotRequired[bool]
    r"""Whether to read information from private channels that the bot is already in.  If false, only public channels will be read.  If true, the bot must be manually added to private channels."""
    join_channels: NotRequired[bool]
    r"""Whether to join all channels or to sync data only from channels the bot is already in.  If false, you''ll need to manually add the bot to all the channels from which you''d like to sync messages."""
    lookback_window: NotRequired[int]
    r"""How far into the past to look for messages in threads, default is 0 days"""
    num_workers: NotRequired[int]
    r"""The number of worker threads to use for the sync."""
    source_type: SlackEnum


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceSlack(BaseModel):
    start_date: datetime
    r"""UTC date and time in the format 2017-01-25T00:00:00Z. Any data before this date will not be replicated."""

    channel_filter: Optional[List[str]] = None
    r"""A channel name list (without leading '#' char) which limit the channels from which you'd like to sync. Empty list means no filter."""

    channel_messages_window_size: Optional[int] = 100
    r"""The size (in days) of the date window that will be used while syncing data from the channel messages stream. A smaller window will allow for greater parallelization when syncing records, but can lead to rate limiting errors."""

    credentials: Optional[SourceSlackAuthenticationMechanism] = None
    r"""Choose how to authenticate into Slack"""

    include_private_channels: Optional[bool] = False
    r"""Whether to read information from private channels that the bot is already in.  If false, only public channels will be read.  If true, the bot must be manually added to private channels."""

    join_channels: Optional[bool] = True
    r"""Whether to join all channels or to sync data only from channels the bot is already in.  If false, you''ll need to manually add the bot to all the channels from which you''d like to sync messages."""

    lookback_window: Optional[int] = 0
    r"""How far into the past to look for messages in threads, default is 0 days"""

    num_workers: Optional[int] = 2
    r"""The number of worker threads to use for the sync."""

    SOURCE_TYPE: Annotated[
        Annotated[SlackEnum, AfterValidator(validate_const(SlackEnum.SLACK))],
        pydantic.Field(alias="sourceType"),
    ] = SlackEnum.SLACK

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "channel_filter",
                "channel_messages_window_size",
                "credentials",
                "include_private_channels",
                "join_channels",
                "lookback_window",
                "num_workers",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceSlackAPIToken.model_rebuild()
except NameError:
    pass
try:
    SignInViaSlackOAuth.model_rebuild()
except NameError:
    pass
try:
    SourceSlack.model_rebuild()
except NameError:
    pass
