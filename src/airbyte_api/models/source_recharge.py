"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Recharge(str, Enum):
    RECHARGE = "recharge"


class SourceRechargeTypedDict(TypedDict):
    access_token: str
    r"""The value of the Access Token generated. See the <a href=\"https://docs.airbyte.com/integrations/sources/recharge\">docs</a> for more information."""
    start_date: datetime
    r"""The date from which you'd like to replicate data for Recharge API, in the format YYYY-MM-DDT00:00:00Z. Any data before this date will not be replicated."""
    lookback_window_days: NotRequired[int]
    r"""Specifies how many days of historical data should be reloaded each time the recharge connector runs."""
    source_type: Recharge
    use_orders_deprecated_api: NotRequired[bool]
    r"""Define whether or not the `Orders` stream should use the deprecated `2021-01` API version, or use `2021-11`, otherwise."""


class SourceRecharge(BaseModel):
    access_token: str
    r"""The value of the Access Token generated. See the <a href=\"https://docs.airbyte.com/integrations/sources/recharge\">docs</a> for more information."""

    start_date: datetime
    r"""The date from which you'd like to replicate data for Recharge API, in the format YYYY-MM-DDT00:00:00Z. Any data before this date will not be replicated."""

    lookback_window_days: Optional[int] = 0
    r"""Specifies how many days of historical data should be reloaded each time the recharge connector runs."""

    SOURCE_TYPE: Annotated[
        Annotated[Recharge, AfterValidator(validate_const(Recharge.RECHARGE))],
        pydantic.Field(alias="sourceType"),
    ] = Recharge.RECHARGE

    use_orders_deprecated_api: Optional[bool] = True
    r"""Define whether or not the `Orders` stream should use the deprecated `2021-01` API version, or use `2021-11`, otherwise."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["lookback_window_days", "use_orders_deprecated_api"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceRecharge.model_rebuild()
except NameError:
    pass
