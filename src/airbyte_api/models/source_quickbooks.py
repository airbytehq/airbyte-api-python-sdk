"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class SourceQuickbooksAuthType(str, Enum):
    OAUTH2_0 = "oauth2.0"


class Quickbooks(str, Enum):
    QUICKBOOKS = "quickbooks"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceQuickbooksTypedDict(TypedDict):
    access_token: str
    r"""Access token for making authenticated requests."""
    client_id: str
    r"""Identifies which app is making the request. Obtain this value from the Keys tab on the app profile via My Apps on the developer site. There are two versions of this key: development and production."""
    client_secret: str
    r"""Obtain this value from the Keys tab on the app profile via My Apps on the developer site. There are two versions of this key: development and production."""
    realm_id: str
    r"""Labeled Company ID. The Make API Calls panel is populated with the realm id and the current access token."""
    refresh_token: str
    r"""A token used when refreshing the access token."""
    start_date: datetime
    r"""The default value to use if no bookmark exists for an endpoint (rfc3339 date string). E.g, 2021-03-20T00:00:00Z. Any data before this date will not be replicated."""
    token_expiry_date: datetime
    r"""The date-time when the access token should be refreshed."""
    auth_type: SourceQuickbooksAuthType
    sandbox: NotRequired[bool]
    r"""Determines whether to use the sandbox or production environment."""
    source_type: Quickbooks


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceQuickbooks(BaseModel):
    access_token: str
    r"""Access token for making authenticated requests."""

    client_id: str
    r"""Identifies which app is making the request. Obtain this value from the Keys tab on the app profile via My Apps on the developer site. There are two versions of this key: development and production."""

    client_secret: str
    r"""Obtain this value from the Keys tab on the app profile via My Apps on the developer site. There are two versions of this key: development and production."""

    realm_id: str
    r"""Labeled Company ID. The Make API Calls panel is populated with the realm id and the current access token."""

    refresh_token: str
    r"""A token used when refreshing the access token."""

    start_date: datetime
    r"""The default value to use if no bookmark exists for an endpoint (rfc3339 date string). E.g, 2021-03-20T00:00:00Z. Any data before this date will not be replicated."""

    token_expiry_date: datetime
    r"""The date-time when the access token should be refreshed."""

    AUTH_TYPE: Annotated[
        Annotated[
            Optional[SourceQuickbooksAuthType],
            AfterValidator(validate_const(SourceQuickbooksAuthType.OAUTH2_0)),
        ],
        pydantic.Field(alias="auth_type"),
    ] = SourceQuickbooksAuthType.OAUTH2_0

    sandbox: Optional[bool] = False
    r"""Determines whether to use the sandbox or production environment."""

    SOURCE_TYPE: Annotated[
        Annotated[Quickbooks, AfterValidator(validate_const(Quickbooks.QUICKBOOKS))],
        pydantic.Field(alias="sourceType"),
    ] = Quickbooks.QUICKBOOKS

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["auth_type", "sandbox"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceQuickbooks.model_rebuild()
except NameError:
    pass
