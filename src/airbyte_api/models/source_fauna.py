"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import get_discriminator, validate_const
from enum import Enum
import pydantic
from pydantic import Discriminator, Tag, model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class DeletionModeDeletionMode(str, Enum):
    DELETED_FIELD = "deleted_field"


class EnabledTypedDict(TypedDict):
    column: NotRequired[str]
    r"""Name of the \"deleted at\" column."""
    deletion_mode: DeletionModeDeletionMode


class Enabled(BaseModel):
    column: Optional[str] = "deleted_at"
    r"""Name of the \"deleted at\" column."""

    DELETION_MODE: Annotated[
        Annotated[
            DeletionModeDeletionMode,
            AfterValidator(validate_const(DeletionModeDeletionMode.DELETED_FIELD)),
        ],
        pydantic.Field(alias="deletion_mode"),
    ] = DeletionModeDeletionMode.DELETED_FIELD

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["column"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class SourceFaunaDeletionModeDeletionMode(str, Enum):
    IGNORE = "ignore"


class DisabledTypedDict(TypedDict):
    deletion_mode: SourceFaunaDeletionModeDeletionMode


class Disabled(BaseModel):
    DELETION_MODE: Annotated[
        Annotated[
            SourceFaunaDeletionModeDeletionMode,
            AfterValidator(validate_const(SourceFaunaDeletionModeDeletionMode.IGNORE)),
        ],
        pydantic.Field(alias="deletion_mode"),
    ] = SourceFaunaDeletionModeDeletionMode.IGNORE


DeletionModeTypedDict = TypeAliasType(
    "DeletionModeTypedDict", Union[DisabledTypedDict, EnabledTypedDict]
)
r"""<b>This only applies to incremental syncs.</b> <br>
Enabling deletion mode informs your destination of deleted documents.<br>
Disabled - Leave this feature disabled, and ignore deleted documents.<br>
Enabled - Enables this feature. When a document is deleted, the connector exports a record with a \"deleted at\" column containing the time that the document was deleted.
"""


DeletionMode = Annotated[
    Union[Annotated[Disabled, Tag("ignore")], Annotated[Enabled, Tag("deleted_field")]],
    Discriminator(lambda m: get_discriminator(m, "deletion_mode", "deletion_mode")),
]
r"""<b>This only applies to incremental syncs.</b> <br>
Enabling deletion mode informs your destination of deleted documents.<br>
Disabled - Leave this feature disabled, and ignore deleted documents.<br>
Enabled - Enables this feature. When a document is deleted, the connector exports a record with a \"deleted at\" column containing the time that the document was deleted.
"""


class CollectionTypedDict(TypedDict):
    r"""Settings for the Fauna Collection."""

    deletions: DeletionModeTypedDict
    r"""<b>This only applies to incremental syncs.</b> <br>
    Enabling deletion mode informs your destination of deleted documents.<br>
    Disabled - Leave this feature disabled, and ignore deleted documents.<br>
    Enabled - Enables this feature. When a document is deleted, the connector exports a record with a \"deleted at\" column containing the time that the document was deleted.
    """
    page_size: NotRequired[int]
    r"""The page size used when reading documents from the database. The larger the page size, the faster the connector processes documents. However, if a page is too large, the connector may fail. <br>
    Choose your page size based on how large the documents are. <br>
    See <a href=\"https://docs.fauna.com/fauna/current/learn/understanding/types#page\">the docs</a>.
    """


class Collection(BaseModel):
    r"""Settings for the Fauna Collection."""

    deletions: DeletionMode
    r"""<b>This only applies to incremental syncs.</b> <br>
    Enabling deletion mode informs your destination of deleted documents.<br>
    Disabled - Leave this feature disabled, and ignore deleted documents.<br>
    Enabled - Enables this feature. When a document is deleted, the connector exports a record with a \"deleted at\" column containing the time that the document was deleted.
    """

    page_size: Optional[int] = 64
    r"""The page size used when reading documents from the database. The larger the page size, the faster the connector processes documents. However, if a page is too large, the connector may fail. <br>
    Choose your page size based on how large the documents are. <br>
    See <a href=\"https://docs.fauna.com/fauna/current/learn/understanding/types#page\">the docs</a>.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["page_size"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class Fauna(str, Enum):
    FAUNA = "fauna"


class SourceFaunaTypedDict(TypedDict):
    secret: str
    r"""Fauna secret, used when authenticating with the database."""
    collection: NotRequired[CollectionTypedDict]
    r"""Settings for the Fauna Collection."""
    domain: NotRequired[str]
    r"""Domain of Fauna to query. Defaults db.fauna.com. See <a href=https://docs.fauna.com/fauna/current/learn/understanding/region_groups#how-to-use-region-groups>the docs</a>."""
    port: NotRequired[int]
    r"""Endpoint port."""
    scheme: NotRequired[str]
    r"""URL scheme."""
    source_type: Fauna


class SourceFauna(BaseModel):
    secret: str
    r"""Fauna secret, used when authenticating with the database."""

    collection: Optional[Collection] = None
    r"""Settings for the Fauna Collection."""

    domain: Optional[str] = "db.fauna.com"
    r"""Domain of Fauna to query. Defaults db.fauna.com. See <a href=https://docs.fauna.com/fauna/current/learn/understanding/region_groups#how-to-use-region-groups>the docs</a>."""

    port: Optional[int] = 443
    r"""Endpoint port."""

    scheme: Optional[str] = "https"
    r"""URL scheme."""

    SOURCE_TYPE: Annotated[
        Annotated[Fauna, AfterValidator(validate_const(Fauna.FAUNA))],
        pydantic.Field(alias="sourceType"),
    ] = Fauna.FAUNA

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["collection", "domain", "port", "scheme"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    Enabled.model_rebuild()
except NameError:
    pass
try:
    Disabled.model_rebuild()
except NameError:
    pass
try:
    SourceFauna.model_rebuild()
except NameError:
    pass
