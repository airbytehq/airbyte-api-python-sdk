"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import get_discriminator, validate_const
from enum import Enum
import pydantic
from pydantic import ConfigDict, Discriminator, Tag, model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Dict, List, Optional, Union
from typing_extensions import (
    Annotated,
    NotRequired,
    TypeAliasType,
    TypedDict,
    deprecated,
)


class ClusterTypeSelfManagedReplicaSet(str, Enum):
    SELF_MANAGED_REPLICA_SET = "SELF_MANAGED_REPLICA_SET"


class SelfManagedReplicaSetTypedDict(TypedDict):
    r"""MongoDB self-hosted cluster configured as a replica set"""

    connection_string: str
    r"""The connection string of the cluster that you want to replicate.  https://www.mongodb.com/docs/manual/reference/connection-string/#find-your-self-hosted-deployment-s-connection-string for more information."""
    databases: List[str]
    r"""The names of the MongoDB databases that contain the collection(s) to replicate."""
    auth_source: NotRequired[str]
    r"""The authentication source where the user information is stored."""
    cluster_type: ClusterTypeSelfManagedReplicaSet
    password: NotRequired[str]
    r"""The password associated with this username."""
    schema_enforced: NotRequired[bool]
    r"""When enabled, syncs will validate and structure records against the stream's schema."""
    username: NotRequired[str]
    r"""The username which is used to access the database."""


class SelfManagedReplicaSet(BaseModel):
    r"""MongoDB self-hosted cluster configured as a replica set"""

    model_config = ConfigDict(
        populate_by_name=True, arbitrary_types_allowed=True, extra="allow"
    )
    __pydantic_extra__: Dict[str, Any] = pydantic.Field(init=False)

    connection_string: str
    r"""The connection string of the cluster that you want to replicate.  https://www.mongodb.com/docs/manual/reference/connection-string/#find-your-self-hosted-deployment-s-connection-string for more information."""

    databases: List[str]
    r"""The names of the MongoDB databases that contain the collection(s) to replicate."""

    auth_source: Optional[str] = "admin"
    r"""The authentication source where the user information is stored."""

    CLUSTER_TYPE: Annotated[
        Annotated[
            ClusterTypeSelfManagedReplicaSet,
            AfterValidator(
                validate_const(
                    ClusterTypeSelfManagedReplicaSet.SELF_MANAGED_REPLICA_SET
                )
            ),
        ],
        pydantic.Field(alias="cluster_type"),
    ] = ClusterTypeSelfManagedReplicaSet.SELF_MANAGED_REPLICA_SET

    password: Optional[str] = None
    r"""The password associated with this username."""

    schema_enforced: Optional[bool] = True
    r"""When enabled, syncs will validate and structure records against the stream's schema."""

    username: Optional[str] = None
    r"""The username which is used to access the database."""

    @property
    def additional_properties(self):
        return self.__pydantic_extra__

    @additional_properties.setter
    def additional_properties(self, value):
        self.__pydantic_extra__ = value  # pyright: ignore[reportIncompatibleVariableOverride]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["auth_source", "password", "schema_enforced", "username"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            serialized.pop(k, serialized.pop(n, None))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val
        for k, v in serialized.items():
            m[k] = v

        return m


class ClusterTypeAtlasReplicaSet(str, Enum):
    ATLAS_REPLICA_SET = "ATLAS_REPLICA_SET"


class MongoDBAtlasReplicaSetTypedDict(TypedDict):
    r"""MongoDB Atlas-hosted cluster configured as a replica set"""

    connection_string: str
    r"""The connection string of the cluster that you want to replicate."""
    databases: List[str]
    r"""The names of the MongoDB databases that contain the collection(s) to replicate."""
    password: str
    r"""The password associated with this username."""
    username: str
    r"""The username which is used to access the database."""
    auth_source: NotRequired[str]
    r"""The authentication source where the user information is stored.  See https://www.mongodb.com/docs/manual/reference/connection-string/#mongodb-urioption-urioption.authSource for more details."""
    cluster_type: ClusterTypeAtlasReplicaSet
    schema_enforced: NotRequired[bool]
    r"""When enabled, syncs will validate and structure records against the stream's schema."""


class MongoDBAtlasReplicaSet(BaseModel):
    r"""MongoDB Atlas-hosted cluster configured as a replica set"""

    model_config = ConfigDict(
        populate_by_name=True, arbitrary_types_allowed=True, extra="allow"
    )
    __pydantic_extra__: Dict[str, Any] = pydantic.Field(init=False)

    connection_string: str
    r"""The connection string of the cluster that you want to replicate."""

    databases: List[str]
    r"""The names of the MongoDB databases that contain the collection(s) to replicate."""

    password: str
    r"""The password associated with this username."""

    username: str
    r"""The username which is used to access the database."""

    auth_source: Optional[str] = "admin"
    r"""The authentication source where the user information is stored.  See https://www.mongodb.com/docs/manual/reference/connection-string/#mongodb-urioption-urioption.authSource for more details."""

    CLUSTER_TYPE: Annotated[
        Annotated[
            ClusterTypeAtlasReplicaSet,
            AfterValidator(
                validate_const(ClusterTypeAtlasReplicaSet.ATLAS_REPLICA_SET)
            ),
        ],
        pydantic.Field(alias="cluster_type"),
    ] = ClusterTypeAtlasReplicaSet.ATLAS_REPLICA_SET

    schema_enforced: Optional[bool] = True
    r"""When enabled, syncs will validate and structure records against the stream's schema."""

    @property
    def additional_properties(self):
        return self.__pydantic_extra__

    @additional_properties.setter
    def additional_properties(self, value):
        self.__pydantic_extra__ = value  # pyright: ignore[reportIncompatibleVariableOverride]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["auth_source", "schema_enforced"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            serialized.pop(k, serialized.pop(n, None))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val
        for k, v in serialized.items():
            m[k] = v

        return m


ClusterTypeTypedDict = TypeAliasType(
    "ClusterTypeTypedDict",
    Union[MongoDBAtlasReplicaSetTypedDict, SelfManagedReplicaSetTypedDict],
)
r"""Configures the MongoDB cluster type."""


ClusterType = Annotated[
    Union[
        Annotated[MongoDBAtlasReplicaSet, Tag("ATLAS_REPLICA_SET")],
        Annotated[SelfManagedReplicaSet, Tag("SELF_MANAGED_REPLICA_SET")],
    ],
    Discriminator(lambda m: get_discriminator(m, "cluster_type", "cluster_type")),
]
r"""Configures the MongoDB cluster type."""


class SourceMongodbV2InvalidCDCPositionBehaviorAdvanced(str, Enum):
    r"""Determines whether Airbyte should fail or re-sync data in case of an stale/invalid cursor value into the WAL. If 'Fail sync' is chosen, a user will have to manually reset the connection before being able to continue syncing data. If 'Re-sync data' is chosen, Airbyte will automatically trigger a refresh but could lead to higher cloud costs and data loss."""

    FAIL_SYNC = "Fail sync"
    RE_SYNC_DATA = "Re-sync data"


class MongodbV2(str, Enum):
    MONGODB_V2 = "mongodb-v2"


class CaptureModeAdvanced(str, Enum):
    r"""Determines how Airbyte looks up the value of an updated document. If 'Lookup' is chosen, the current value of the document will be read. If 'Post Image' is chosen, then the version of the document immediately after an update will be read. WARNING : Severe data loss will occur if this option is chosen and the appropriate settings are not set on your Mongo instance : https://www.mongodb.com/docs/manual/changeStreams/#change-streams-with-document-pre-and-post-images."""

    LOOKUP = "Lookup"
    POST_IMAGE = "Post Image"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceMongodbV2TypedDict(TypedDict):
    database_config: ClusterTypeTypedDict
    r"""Configures the MongoDB cluster type."""
    discover_sample_size: NotRequired[int]
    r"""The maximum number of documents to sample when attempting to discover the unique fields for a collection."""
    discover_timeout_seconds: NotRequired[int]
    r"""The amount of time the connector will wait when it discovers a document. Defaults to 600 seconds. Valid range: 5 seconds to 1200 seconds."""
    initial_load_timeout_hours: NotRequired[int]
    r"""The amount of time an initial load is allowed to continue for before catching up on CDC logs."""
    initial_waiting_seconds: NotRequired[int]
    r"""The amount of time the connector will wait when it launches to determine if there is new data to sync or not. Defaults to 300 seconds. Valid range: 120 seconds to 1200 seconds."""
    invalid_cdc_cursor_position_behavior: NotRequired[
        SourceMongodbV2InvalidCDCPositionBehaviorAdvanced
    ]
    r"""Determines whether Airbyte should fail or re-sync data in case of an stale/invalid cursor value into the WAL. If 'Fail sync' is chosen, a user will have to manually reset the connection before being able to continue syncing data. If 'Re-sync data' is chosen, Airbyte will automatically trigger a refresh but could lead to higher cloud costs and data loss."""
    queue_size: NotRequired[int]
    r"""The size of the internal queue. This may interfere with memory consumption and efficiency of the connector, please be careful."""
    source_type: MongodbV2
    update_capture_mode: NotRequired[CaptureModeAdvanced]
    r"""Determines how Airbyte looks up the value of an updated document. If 'Lookup' is chosen, the current value of the document will be read. If 'Post Image' is chosen, then the version of the document immediately after an update will be read. WARNING : Severe data loss will occur if this option is chosen and the appropriate settings are not set on your Mongo instance : https://www.mongodb.com/docs/manual/changeStreams/#change-streams-with-document-pre-and-post-images."""


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceMongodbV2(BaseModel):
    database_config: ClusterType
    r"""Configures the MongoDB cluster type."""

    discover_sample_size: Optional[int] = 10000
    r"""The maximum number of documents to sample when attempting to discover the unique fields for a collection."""

    discover_timeout_seconds: Optional[int] = 600
    r"""The amount of time the connector will wait when it discovers a document. Defaults to 600 seconds. Valid range: 5 seconds to 1200 seconds."""

    initial_load_timeout_hours: Optional[int] = 8
    r"""The amount of time an initial load is allowed to continue for before catching up on CDC logs."""

    initial_waiting_seconds: Optional[int] = 300
    r"""The amount of time the connector will wait when it launches to determine if there is new data to sync or not. Defaults to 300 seconds. Valid range: 120 seconds to 1200 seconds."""

    invalid_cdc_cursor_position_behavior: Optional[
        SourceMongodbV2InvalidCDCPositionBehaviorAdvanced
    ] = SourceMongodbV2InvalidCDCPositionBehaviorAdvanced.FAIL_SYNC
    r"""Determines whether Airbyte should fail or re-sync data in case of an stale/invalid cursor value into the WAL. If 'Fail sync' is chosen, a user will have to manually reset the connection before being able to continue syncing data. If 'Re-sync data' is chosen, Airbyte will automatically trigger a refresh but could lead to higher cloud costs and data loss."""

    queue_size: Optional[int] = 10000
    r"""The size of the internal queue. This may interfere with memory consumption and efficiency of the connector, please be careful."""

    SOURCE_TYPE: Annotated[
        Annotated[MongodbV2, AfterValidator(validate_const(MongodbV2.MONGODB_V2))],
        pydantic.Field(alias="sourceType"),
    ] = MongodbV2.MONGODB_V2

    update_capture_mode: Optional[CaptureModeAdvanced] = CaptureModeAdvanced.LOOKUP
    r"""Determines how Airbyte looks up the value of an updated document. If 'Lookup' is chosen, the current value of the document will be read. If 'Post Image' is chosen, then the version of the document immediately after an update will be read. WARNING : Severe data loss will occur if this option is chosen and the appropriate settings are not set on your Mongo instance : https://www.mongodb.com/docs/manual/changeStreams/#change-streams-with-document-pre-and-post-images."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "discover_sample_size",
                "discover_timeout_seconds",
                "initial_load_timeout_hours",
                "initial_waiting_seconds",
                "invalid_cdc_cursor_position_behavior",
                "queue_size",
                "update_capture_mode",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SelfManagedReplicaSet.model_rebuild()
except NameError:
    pass
try:
    MongoDBAtlasReplicaSet.model_rebuild()
except NameError:
    pass
try:
    SourceMongodbV2.model_rebuild()
except NameError:
    pass
