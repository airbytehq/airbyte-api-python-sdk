"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class ServiceDetail(str, Enum):
    ESCALATION_POLICIES = "escalation_policies"
    TEAMS = "teams"
    INTEGRATIONS = "integrations"
    AUTO_PAUSE_NOTIFICATIONS_PARAMETERS = "auto_pause_notifications_parameters"


class Pagerduty(str, Enum):
    PAGERDUTY = "pagerduty"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourcePagerdutyTypedDict(TypedDict):
    token: str
    r"""API key for PagerDuty API authentication"""
    cutoff_days: NotRequired[int]
    r"""Fetch pipelines updated in the last number of days"""
    default_severity: NotRequired[str]
    r"""A default severity category if not present"""
    exclude_services: NotRequired[List[str]]
    r"""List of PagerDuty service names to ignore incidents from. If not set, all incidents will be pulled."""
    incident_log_entries_overview: NotRequired[bool]
    r"""If true, will return a subset of log entries that show only the most important changes to the incident."""
    max_retries: NotRequired[int]
    r"""Maximum number of PagerDuty API request retries to perform upon connection errors. The source will pause for an exponentially increasing number of seconds before retrying."""
    page_size: NotRequired[int]
    r"""page size to use when querying PagerDuty API"""
    service_details: NotRequired[List[ServiceDetail]]
    r"""List of PagerDuty service additional details to include."""
    source_type: Pagerduty


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourcePagerduty(BaseModel):
    token: str
    r"""API key for PagerDuty API authentication"""

    cutoff_days: Optional[int] = 90
    r"""Fetch pipelines updated in the last number of days"""

    default_severity: Optional[str] = None
    r"""A default severity category if not present"""

    exclude_services: Optional[List[str]] = None
    r"""List of PagerDuty service names to ignore incidents from. If not set, all incidents will be pulled."""

    incident_log_entries_overview: Optional[bool] = True
    r"""If true, will return a subset of log entries that show only the most important changes to the incident."""

    max_retries: Optional[int] = 5
    r"""Maximum number of PagerDuty API request retries to perform upon connection errors. The source will pause for an exponentially increasing number of seconds before retrying."""

    page_size: Optional[int] = 25
    r"""page size to use when querying PagerDuty API"""

    service_details: Optional[List[ServiceDetail]] = None
    r"""List of PagerDuty service additional details to include."""

    SOURCE_TYPE: Annotated[
        Annotated[Pagerduty, AfterValidator(validate_const(Pagerduty.PAGERDUTY))],
        pydantic.Field(alias="sourceType"),
    ] = Pagerduty.PAGERDUTY

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "cutoff_days",
                "default_severity",
                "exclude_services",
                "incident_log_entries_overview",
                "max_retries",
                "page_size",
                "service_details",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourcePagerduty.model_rebuild()
except NameError:
    pass
