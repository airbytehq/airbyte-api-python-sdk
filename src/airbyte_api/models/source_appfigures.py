"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class GroupBy(str, Enum):
    r"""Category term for grouping the search results"""

    NETWORK = "network"
    PRODUCT = "product"
    COUNTRY = "country"
    DATE = "date"


class Appfigures(str, Enum):
    APPFIGURES = "appfigures"


class SourceAppfiguresTypedDict(TypedDict):
    api_key: str
    start_date: datetime
    group_by: NotRequired[GroupBy]
    r"""Category term for grouping the search results"""
    search_store: NotRequired[str]
    r"""The store which needs to be searched in streams"""
    source_type: Appfigures


class SourceAppfigures(BaseModel):
    api_key: str

    start_date: datetime

    group_by: Optional[GroupBy] = GroupBy.PRODUCT
    r"""Category term for grouping the search results"""

    search_store: Optional[str] = "apple"
    r"""The store which needs to be searched in streams"""

    SOURCE_TYPE: Annotated[
        Annotated[Appfigures, AfterValidator(validate_const(Appfigures.APPFIGURES))],
        pydantic.Field(alias="sourceType"),
    ] = Appfigures.APPFIGURES

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["group_by", "search_store"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceAppfigures.model_rebuild()
except NameError:
    pass
