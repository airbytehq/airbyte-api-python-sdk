"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class TvmazeSchedule(str, Enum):
    TVMAZE_SCHEDULE = "tvmaze-schedule"


class SourceTvmazeScheduleTypedDict(TypedDict):
    domestic_schedule_country_code: str
    r"""Country code for domestic TV schedule retrieval."""
    start_date: str
    r"""Start date for TV schedule retrieval. May be in the future."""
    end_date: NotRequired[str]
    r"""End date for TV schedule retrieval. May be in the future. Optional.

    """
    source_type: TvmazeSchedule
    web_schedule_country_code: NotRequired[str]
    r"""ISO 3166-1 country code for web TV schedule retrieval. Leave blank for
    all countries plus global web channels (e.g. Netflix). Alternatively,
    set to 'global' for just global web channels.

    """


class SourceTvmazeSchedule(BaseModel):
    domestic_schedule_country_code: str
    r"""Country code for domestic TV schedule retrieval."""

    start_date: str
    r"""Start date for TV schedule retrieval. May be in the future."""

    end_date: Optional[str] = None
    r"""End date for TV schedule retrieval. May be in the future. Optional.

    """

    SOURCE_TYPE: Annotated[
        Annotated[
            TvmazeSchedule,
            AfterValidator(validate_const(TvmazeSchedule.TVMAZE_SCHEDULE)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = TvmazeSchedule.TVMAZE_SCHEDULE

    web_schedule_country_code: Optional[str] = None
    r"""ISO 3166-1 country code for web TV schedule retrieval. Leave blank for
    all countries plus global web channels (e.g. Netflix). Alternatively,
    set to 'global' for just global web channels.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["end_date", "web_schedule_country_code"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceTvmazeSchedule.model_rebuild()
except NameError:
    pass
