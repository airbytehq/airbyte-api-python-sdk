"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Segment(str, Enum):
    SEGMENT = "segment"


class SourceSegmentTypedDict(TypedDict):
    api_token: str
    r"""API token to use. Generate it in Segment's Workspace settings."""
    start_date: datetime
    region: NotRequired[str]
    r"""The region for the API, e.g., 'api' for US or 'eu1' for EU"""
    source_type: Segment


class SourceSegment(BaseModel):
    api_token: str
    r"""API token to use. Generate it in Segment's Workspace settings."""

    start_date: datetime

    region: Optional[str] = "api"
    r"""The region for the API, e.g., 'api' for US or 'eu1' for EU"""

    SOURCE_TYPE: Annotated[
        Annotated[Segment, AfterValidator(validate_const(Segment.SEGMENT))],
        pydantic.Field(alias="sourceType"),
    ] = Segment.SEGMENT

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["region"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceSegment.model_rebuild()
except NameError:
    pass
