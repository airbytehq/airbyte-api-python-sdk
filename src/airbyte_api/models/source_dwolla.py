"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class SourceDwollaEnvironment(str, Enum):
    r"""The environment for the Dwolla API, either 'api-sandbox' or 'api'."""

    API = "api"
    API_SANDBOX = "api-sandbox"


class Dwolla(str, Enum):
    DWOLLA = "dwolla"


class SourceDwollaTypedDict(TypedDict):
    client_id: str
    client_secret: str
    start_date: datetime
    environment: NotRequired[SourceDwollaEnvironment]
    r"""The environment for the Dwolla API, either 'api-sandbox' or 'api'."""
    source_type: Dwolla


class SourceDwolla(BaseModel):
    client_id: str

    client_secret: str

    start_date: datetime

    environment: Optional[SourceDwollaEnvironment] = SourceDwollaEnvironment.API
    r"""The environment for the Dwolla API, either 'api-sandbox' or 'api'."""

    SOURCE_TYPE: Annotated[
        Annotated[Dwolla, AfterValidator(validate_const(Dwolla.DWOLLA))],
        pydantic.Field(alias="sourceType"),
    ] = Dwolla.DWOLLA

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["environment"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceDwolla.model_rebuild()
except NameError:
    pass
