"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import get_discriminator, validate_const
from enum import Enum
import pydantic
from pydantic import Discriminator, Tag, model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional, Union
from typing_extensions import (
    Annotated,
    NotRequired,
    TypeAliasType,
    TypedDict,
    deprecated,
)


class FileFormat(str, Enum):
    r"""The Format of the file which should be replicated (Warning: some formats may be experimental, please refer to the docs)."""

    CSV = "csv"
    JSON = "json"
    JSONL = "jsonl"
    EXCEL = "excel"
    EXCEL_BINARY = "excel_binary"
    FWF = "fwf"
    FEATHER = "feather"
    PARQUET = "parquet"
    YAML = "yaml"


class StorageLocal(str, Enum):
    r"""WARNING: Note that the local storage URL available for reading must start with the local mount \"/local/\" at the moment until we implement more advanced docker mounting options."""

    LOCAL = "local"


class LocalFilesystemLimitedTypedDict(TypedDict):
    storage: StorageLocal
    r"""WARNING: Note that the local storage URL available for reading must start with the local mount \"/local/\" at the moment until we implement more advanced docker mounting options."""


class LocalFilesystemLimited(BaseModel):
    STORAGE: Annotated[
        Annotated[StorageLocal, AfterValidator(validate_const(StorageLocal.LOCAL))],
        pydantic.Field(alias="storage"),
    ] = StorageLocal.LOCAL
    r"""WARNING: Note that the local storage URL available for reading must start with the local mount \"/local/\" at the moment until we implement more advanced docker mounting options."""


class StorageSftp(str, Enum):
    SFTP = "SFTP"


class SFTPSecureFileTransferProtocolTypedDict(TypedDict):
    host: str
    user: str
    password: NotRequired[str]
    port: NotRequired[str]
    storage: StorageSftp


class SFTPSecureFileTransferProtocol(BaseModel):
    host: str

    user: str

    password: Optional[str] = None

    port: Optional[str] = "22"

    STORAGE: Annotated[
        Annotated[StorageSftp, AfterValidator(validate_const(StorageSftp.SFTP))],
        pydantic.Field(alias="storage"),
    ] = StorageSftp.SFTP

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["password", "port"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class StorageScp(str, Enum):
    SCP = "SCP"


class SCPSecureCopyProtocolTypedDict(TypedDict):
    host: str
    user: str
    password: NotRequired[str]
    port: NotRequired[str]
    storage: StorageScp


class SCPSecureCopyProtocol(BaseModel):
    host: str

    user: str

    password: Optional[str] = None

    port: Optional[str] = "22"

    STORAGE: Annotated[
        Annotated[StorageScp, AfterValidator(validate_const(StorageScp.SCP))],
        pydantic.Field(alias="storage"),
    ] = StorageScp.SCP

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["password", "port"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class StorageSSH(str, Enum):
    SSH = "SSH"


class SSHSecureShellTypedDict(TypedDict):
    host: str
    user: str
    password: NotRequired[str]
    port: NotRequired[str]
    storage: StorageSSH


class SSHSecureShell(BaseModel):
    host: str

    user: str

    password: Optional[str] = None

    port: Optional[str] = "22"

    STORAGE: Annotated[
        Annotated[StorageSSH, AfterValidator(validate_const(StorageSSH.SSH))],
        pydantic.Field(alias="storage"),
    ] = StorageSSH.SSH

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["password", "port"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class StorageAzBlob(str, Enum):
    AZ_BLOB = "AzBlob"


class AzBlobAzureBlobStorageTypedDict(TypedDict):
    storage_account: str
    r"""The globally unique name of the storage account that the desired blob sits within. See <a href=\"https://docs.microsoft.com/en-us/azure/storage/common/storage-account-overview\" target=\"_blank\">here</a> for more details."""
    sas_token: NotRequired[str]
    r"""To access Azure Blob Storage, this connector would need credentials with the proper permissions. One option is a SAS (Shared Access Signature) token. If accessing publicly available data, this field is not necessary."""
    shared_key: NotRequired[str]
    r"""To access Azure Blob Storage, this connector would need credentials with the proper permissions. One option is a storage account shared key (aka account key or access key). If accessing publicly available data, this field is not necessary."""
    storage: StorageAzBlob


class AzBlobAzureBlobStorage(BaseModel):
    storage_account: str
    r"""The globally unique name of the storage account that the desired blob sits within. See <a href=\"https://docs.microsoft.com/en-us/azure/storage/common/storage-account-overview\" target=\"_blank\">here</a> for more details."""

    sas_token: Optional[str] = None
    r"""To access Azure Blob Storage, this connector would need credentials with the proper permissions. One option is a SAS (Shared Access Signature) token. If accessing publicly available data, this field is not necessary."""

    shared_key: Optional[str] = None
    r"""To access Azure Blob Storage, this connector would need credentials with the proper permissions. One option is a storage account shared key (aka account key or access key). If accessing publicly available data, this field is not necessary."""

    STORAGE: Annotated[
        Annotated[StorageAzBlob, AfterValidator(validate_const(StorageAzBlob.AZ_BLOB))],
        pydantic.Field(alias="storage"),
    ] = StorageAzBlob.AZ_BLOB

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["sas_token", "shared_key"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class StorageS3(str, Enum):
    S3 = "S3"


class S3AmazonWebServicesTypedDict(TypedDict):
    aws_access_key_id: NotRequired[str]
    r"""In order to access private Buckets stored on AWS S3, this connector would need credentials with the proper permissions. If accessing publicly available data, this field is not necessary."""
    aws_secret_access_key: NotRequired[str]
    r"""In order to access private Buckets stored on AWS S3, this connector would need credentials with the proper permissions. If accessing publicly available data, this field is not necessary."""
    storage: StorageS3


class S3AmazonWebServices(BaseModel):
    aws_access_key_id: Optional[str] = None
    r"""In order to access private Buckets stored on AWS S3, this connector would need credentials with the proper permissions. If accessing publicly available data, this field is not necessary."""

    aws_secret_access_key: Optional[str] = None
    r"""In order to access private Buckets stored on AWS S3, this connector would need credentials with the proper permissions. If accessing publicly available data, this field is not necessary."""

    STORAGE: Annotated[
        Annotated[StorageS3, AfterValidator(validate_const(StorageS3.S3))],
        pydantic.Field(alias="storage"),
    ] = StorageS3.S3

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["aws_access_key_id", "aws_secret_access_key"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class StorageGcs(str, Enum):
    GCS = "GCS"


class GCSGoogleCloudStorageTypedDict(TypedDict):
    service_account_json: NotRequired[str]
    r"""In order to access private Buckets stored on Google Cloud, this connector would need a service account json credentials with the proper permissions as described <a href=\"https://cloud.google.com/iam/docs/service-accounts\" target=\"_blank\">here</a>. Please generate the credentials.json file and copy/paste its content to this field (expecting JSON formats). If accessing publicly available data, this field is not necessary."""
    storage: StorageGcs


class GCSGoogleCloudStorage(BaseModel):
    service_account_json: Optional[str] = None
    r"""In order to access private Buckets stored on Google Cloud, this connector would need a service account json credentials with the proper permissions as described <a href=\"https://cloud.google.com/iam/docs/service-accounts\" target=\"_blank\">here</a>. Please generate the credentials.json file and copy/paste its content to this field (expecting JSON formats). If accessing publicly available data, this field is not necessary."""

    STORAGE: Annotated[
        Annotated[StorageGcs, AfterValidator(validate_const(StorageGcs.GCS))],
        pydantic.Field(alias="storage"),
    ] = StorageGcs.GCS

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["service_account_json"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class StorageHTTPS(str, Enum):
    HTTPS = "HTTPS"


class HTTPSPublicWebTypedDict(TypedDict):
    storage: StorageHTTPS
    user_agent: NotRequired[bool]
    r"""Add User-Agent to request"""


class HTTPSPublicWeb(BaseModel):
    STORAGE: Annotated[
        Annotated[StorageHTTPS, AfterValidator(validate_const(StorageHTTPS.HTTPS))],
        pydantic.Field(alias="storage"),
    ] = StorageHTTPS.HTTPS

    user_agent: Optional[bool] = False
    r"""Add User-Agent to request"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["user_agent"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


StorageProviderTypedDict = TypeAliasType(
    "StorageProviderTypedDict",
    Union[
        LocalFilesystemLimitedTypedDict,
        HTTPSPublicWebTypedDict,
        GCSGoogleCloudStorageTypedDict,
        S3AmazonWebServicesTypedDict,
        AzBlobAzureBlobStorageTypedDict,
        SSHSecureShellTypedDict,
        SCPSecureCopyProtocolTypedDict,
        SFTPSecureFileTransferProtocolTypedDict,
    ],
)
r"""The storage Provider or Location of the file(s) which should be replicated."""


StorageProvider = Annotated[
    Union[
        Annotated[HTTPSPublicWeb, Tag("HTTPS")],
        Annotated[GCSGoogleCloudStorage, Tag("GCS")],
        Annotated[S3AmazonWebServices, Tag("S3")],
        Annotated[AzBlobAzureBlobStorage, Tag("AzBlob")],
        Annotated[SSHSecureShell, Tag("SSH")],
        Annotated[SCPSecureCopyProtocol, Tag("SCP")],
        Annotated[SFTPSecureFileTransferProtocol, Tag("SFTP")],
        Annotated[LocalFilesystemLimited, Tag("local")],
    ],
    Discriminator(lambda m: get_discriminator(m, "storage", "storage")),
]
r"""The storage Provider or Location of the file(s) which should be replicated."""


class File(str, Enum):
    FILE = "file"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceFileTypedDict(TypedDict):
    dataset_name: str
    r"""The Name of the final table to replicate this file into (should include letters, numbers dash and underscores only)."""
    provider: StorageProviderTypedDict
    r"""The storage Provider or Location of the file(s) which should be replicated."""
    url: str
    r"""The URL path to access the file which should be replicated."""
    format_: NotRequired[FileFormat]
    r"""The Format of the file which should be replicated (Warning: some formats may be experimental, please refer to the docs)."""
    reader_options: NotRequired[str]
    r"""This should be a string in JSON format. It depends on the chosen file format to provide additional options and tune its behavior."""
    source_type: File


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceFile(BaseModel):
    dataset_name: str
    r"""The Name of the final table to replicate this file into (should include letters, numbers dash and underscores only)."""

    provider: StorageProvider
    r"""The storage Provider or Location of the file(s) which should be replicated."""

    url: str
    r"""The URL path to access the file which should be replicated."""

    format_: Annotated[Optional[FileFormat], pydantic.Field(alias="format")] = (
        FileFormat.CSV
    )
    r"""The Format of the file which should be replicated (Warning: some formats may be experimental, please refer to the docs)."""

    reader_options: Optional[str] = None
    r"""This should be a string in JSON format. It depends on the chosen file format to provide additional options and tune its behavior."""

    SOURCE_TYPE: Annotated[
        Annotated[File, AfterValidator(validate_const(File.FILE))],
        pydantic.Field(alias="sourceType"),
    ] = File.FILE

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["format", "reader_options"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    LocalFilesystemLimited.model_rebuild()
except NameError:
    pass
try:
    SFTPSecureFileTransferProtocol.model_rebuild()
except NameError:
    pass
try:
    SCPSecureCopyProtocol.model_rebuild()
except NameError:
    pass
try:
    SSHSecureShell.model_rebuild()
except NameError:
    pass
try:
    AzBlobAzureBlobStorage.model_rebuild()
except NameError:
    pass
try:
    S3AmazonWebServices.model_rebuild()
except NameError:
    pass
try:
    GCSGoogleCloudStorage.model_rebuild()
except NameError:
    pass
try:
    HTTPSPublicWeb.model_rebuild()
except NameError:
    pass
try:
    SourceFile.model_rebuild()
except NameError:
    pass
