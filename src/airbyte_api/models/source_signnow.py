"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Signnow(str, Enum):
    SIGNNOW = "signnow"


class SourceSignnowTypedDict(TypedDict):
    api_key_id: str
    r"""Api key which could be found in API section after enlarging keys section"""
    auth_token: str
    r"""The authorization token is needed for `signing_links` stream which could be seen from enlarged view of `https://app.signnow.com/webapp/api-dashboard/keys`"""
    start_date: datetime
    name_filter_for_documents: NotRequired[List[Any]]
    r"""Name filter for documents stream"""
    source_type: Signnow


class SourceSignnow(BaseModel):
    api_key_id: str
    r"""Api key which could be found in API section after enlarging keys section"""

    auth_token: str
    r"""The authorization token is needed for `signing_links` stream which could be seen from enlarged view of `https://app.signnow.com/webapp/api-dashboard/keys`"""

    start_date: datetime

    name_filter_for_documents: Optional[List[Any]] = None
    r"""Name filter for documents stream"""

    SOURCE_TYPE: Annotated[
        Annotated[Signnow, AfterValidator(validate_const(Signnow.SIGNNOW))],
        pydantic.Field(alias="sourceType"),
    ] = Signnow.SIGNNOW

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name_filter_for_documents"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceSignnow.model_rebuild()
except NameError:
    pass
