"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Mailersend(str, Enum):
    MAILERSEND = "mailersend"


class SourceMailersendTypedDict(TypedDict):
    api_token: str
    r"""Your API Token. See <a href=\"https://www.mailersend.com/help/managing-api-tokens\">here</a>."""
    domain_id: str
    r"""The domain entity in mailersend"""
    source_type: Mailersend
    start_date: NotRequired[float]
    r"""Timestamp is assumed to be UTC."""


class SourceMailersend(BaseModel):
    api_token: str
    r"""Your API Token. See <a href=\"https://www.mailersend.com/help/managing-api-tokens\">here</a>."""

    domain_id: str
    r"""The domain entity in mailersend"""

    SOURCE_TYPE: Annotated[
        Annotated[Mailersend, AfterValidator(validate_const(Mailersend.MAILERSEND))],
        pydantic.Field(alias="sourceType"),
    ] = Mailersend.MAILERSEND

    start_date: Optional[float] = None
    r"""Timestamp is assumed to be UTC."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceMailersend.model_rebuild()
except NameError:
    pass
