"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import ConfigDict, model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Dict, Optional, Union
from typing_extensions import (
    Annotated,
    NotRequired,
    TypeAliasType,
    TypedDict,
    deprecated,
)


class DataTypeTypes(str, Enum):
    TYPES = "types"


class AllTypesTypedDict(TypedDict):
    r"""Generates one column of each Airbyte data type."""

    data_type: NotRequired[DataTypeTypes]


class AllTypes(BaseModel):
    r"""Generates one column of each Airbyte data type."""

    model_config = ConfigDict(
        populate_by_name=True, arbitrary_types_allowed=True, extra="allow"
    )
    __pydantic_extra__: Dict[str, Any] = pydantic.Field(init=False)

    data_type: Optional[DataTypeTypes] = DataTypeTypes.TYPES

    @property
    def additional_properties(self):
        return self.__pydantic_extra__

    @additional_properties.setter
    def additional_properties(self, value):
        self.__pydantic_extra__ = value  # pyright: ignore[reportIncompatibleVariableOverride]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["data_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            serialized.pop(k, serialized.pop(n, None))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val
        for k, v in serialized.items():
            m[k] = v

        return m


class DataTypeIncrement(str, Enum):
    INCREMENT = "increment"


class IncrementalTypedDict(TypedDict):
    r"""Generates incrementally increasing numerical data for the source."""

    data_type: NotRequired[DataTypeIncrement]


class Incremental(BaseModel):
    r"""Generates incrementally increasing numerical data for the source."""

    model_config = ConfigDict(
        populate_by_name=True, arbitrary_types_allowed=True, extra="allow"
    )
    __pydantic_extra__: Dict[str, Any] = pydantic.Field(init=False)

    data_type: Optional[DataTypeIncrement] = DataTypeIncrement.INCREMENT

    @property
    def additional_properties(self):
        return self.__pydantic_extra__

    @additional_properties.setter
    def additional_properties(self, value):
        self.__pydantic_extra__ = value  # pyright: ignore[reportIncompatibleVariableOverride]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["data_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            serialized.pop(k, serialized.pop(n, None))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val
        for k, v in serialized.items():
            m[k] = v

        return m


DataGenerationTypeTypedDict = TypeAliasType(
    "DataGenerationTypeTypedDict", Union[IncrementalTypedDict, AllTypesTypedDict]
)
r"""Different patterns for generating data"""


DataGenerationType = TypeAliasType("DataGenerationType", Union[Incremental, AllTypes])
r"""Different patterns for generating data"""


class Datagen(str, Enum):
    DATAGEN = "datagen"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceDatagenTypedDict(TypedDict):
    flavor: DataGenerationTypeTypedDict
    r"""Different patterns for generating data"""
    concurrency: NotRequired[int]
    r"""Maximum number of concurrent data generators. Leave empty to let Airbyte optimize performance."""
    max_records: NotRequired[int]
    r"""The number of record messages to emit from this connector. Min 1. Max 100 billion."""
    source_type: Datagen


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceDatagen(BaseModel):
    flavor: DataGenerationType
    r"""Different patterns for generating data"""

    concurrency: Optional[int] = None
    r"""Maximum number of concurrent data generators. Leave empty to let Airbyte optimize performance."""

    max_records: Optional[int] = 100
    r"""The number of record messages to emit from this connector. Min 1. Max 100 billion."""

    SOURCE_TYPE: Annotated[
        Annotated[Datagen, AfterValidator(validate_const(Datagen.DATAGEN))],
        pydantic.Field(alias="sourceType"),
    ] = Datagen.DATAGEN

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["concurrency", "max_records"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceDatagen.model_rebuild()
except NameError:
    pass
