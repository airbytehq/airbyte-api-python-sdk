"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class OpenExchangeRates(str, Enum):
    OPEN_EXCHANGE_RATES = "open-exchange-rates"


class SourceOpenExchangeRatesTypedDict(TypedDict):
    app_id: str
    r"""App ID provided by Open Exchange Rates"""
    start_date: str
    r"""Start getting data from that date."""
    base: NotRequired[str]
    r"""Change base currency (3-letter code, default is USD - only modifiable in paid plans)"""
    source_type: OpenExchangeRates


class SourceOpenExchangeRates(BaseModel):
    app_id: str
    r"""App ID provided by Open Exchange Rates"""

    start_date: str
    r"""Start getting data from that date."""

    base: Optional[str] = "USD"
    r"""Change base currency (3-letter code, default is USD - only modifiable in paid plans)"""

    SOURCE_TYPE: Annotated[
        Annotated[
            OpenExchangeRates,
            AfterValidator(validate_const(OpenExchangeRates.OPEN_EXCHANGE_RATES)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = OpenExchangeRates.OPEN_EXCHANGE_RATES

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["base"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceOpenExchangeRates.model_rebuild()
except NameError:
    pass
