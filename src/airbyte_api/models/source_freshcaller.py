"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Freshcaller(str, Enum):
    FRESHCALLER = "freshcaller"


class SourceFreshcallerTypedDict(TypedDict):
    api_key: str
    r"""Freshcaller API Key. See the docs for more information on how to obtain this key."""
    domain: str
    r"""Used to construct Base URL for the Freshcaller APIs"""
    requests_per_minute: NotRequired[int]
    r"""The number of requests per minute that this source allowed to use. There is a rate limit of 50 requests per minute per app per account."""
    source_type: Freshcaller
    start_date: NotRequired[datetime]
    r"""UTC date and time. Any data created after this date will be replicated."""
    sync_lag_minutes: NotRequired[int]
    r"""Lag in minutes for each sync, i.e., at time T, data for the time range [prev_sync_time, T-30] will be fetched"""


class SourceFreshcaller(BaseModel):
    api_key: str
    r"""Freshcaller API Key. See the docs for more information on how to obtain this key."""

    domain: str
    r"""Used to construct Base URL for the Freshcaller APIs"""

    requests_per_minute: Optional[int] = None
    r"""The number of requests per minute that this source allowed to use. There is a rate limit of 50 requests per minute per app per account."""

    SOURCE_TYPE: Annotated[
        Annotated[Freshcaller, AfterValidator(validate_const(Freshcaller.FRESHCALLER))],
        pydantic.Field(alias="sourceType"),
    ] = Freshcaller.FRESHCALLER

    start_date: Optional[datetime] = None
    r"""UTC date and time. Any data created after this date will be replicated."""

    sync_lag_minutes: Optional[int] = None
    r"""Lag in minutes for each sync, i.e., at time T, data for the time range [prev_sync_time, T-30] will be fetched"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["requests_per_minute", "start_date", "sync_lag_minutes"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceFreshcaller.model_rebuild()
except NameError:
    pass
