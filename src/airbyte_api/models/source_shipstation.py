"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Shipstation(str, Enum):
    SHIPSTATION = "shipstation"


class SourceShipstationTypedDict(TypedDict):
    start_date: datetime
    username: str
    password: NotRequired[str]
    source_type: Shipstation


class SourceShipstation(BaseModel):
    start_date: datetime

    username: str

    password: Optional[str] = None

    SOURCE_TYPE: Annotated[
        Annotated[Shipstation, AfterValidator(validate_const(Shipstation.SHIPSTATION))],
        pydantic.Field(alias="sourceType"),
    ] = Shipstation.SHIPSTATION

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["password"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceShipstation.model_rebuild()
except NameError:
    pass
