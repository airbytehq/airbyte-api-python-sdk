"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Metricool(str, Enum):
    METRICOOL = "metricool"


class SourceMetricoolTypedDict(TypedDict):
    blog_ids: List[Any]
    r"""Brand IDs"""
    user_id: str
    r"""Account ID"""
    user_token: str
    r"""User token to authenticate API requests. Find it in the Account Settings menu, API section of your Metricool account."""
    end_date: NotRequired[datetime]
    r"""If not set, defaults to current datetime."""
    source_type: Metricool
    start_date: NotRequired[datetime]
    r"""If not set, defaults to 60 days back. If below \"End Date\", defaults to 1 day before \"End Date\" """


class SourceMetricool(BaseModel):
    blog_ids: List[Any]
    r"""Brand IDs"""

    user_id: str
    r"""Account ID"""

    user_token: str
    r"""User token to authenticate API requests. Find it in the Account Settings menu, API section of your Metricool account."""

    end_date: Optional[datetime] = None
    r"""If not set, defaults to current datetime."""

    SOURCE_TYPE: Annotated[
        Annotated[Metricool, AfterValidator(validate_const(Metricool.METRICOOL))],
        pydantic.Field(alias="sourceType"),
    ] = Metricool.METRICOOL

    start_date: Optional[datetime] = None
    r"""If not set, defaults to 60 days back. If below \"End Date\", defaults to 1 day before \"End Date\" """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["end_date", "start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceMetricool.model_rebuild()
except NameError:
    pass
