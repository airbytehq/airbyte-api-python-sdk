"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class SigmaComputing(str, Enum):
    SIGMA_COMPUTING = "sigma-computing"


class SourceSigmaComputingTypedDict(TypedDict):
    base_url: str
    r"""The base url of your sigma organization"""
    client_id: str
    client_refresh_token: str
    client_secret: str
    oauth_access_token: NotRequired[str]
    r"""The current access token. This field might be overridden by the connector based on the token refresh endpoint response."""
    oauth_token_expiry_date: NotRequired[datetime]
    r"""The date the current access token expires in. This field might be overridden by the connector based on the token refresh endpoint response."""
    source_type: SigmaComputing


class SourceSigmaComputing(BaseModel):
    base_url: str
    r"""The base url of your sigma organization"""

    client_id: str

    client_refresh_token: str

    client_secret: str

    oauth_access_token: Optional[str] = None
    r"""The current access token. This field might be overridden by the connector based on the token refresh endpoint response."""

    oauth_token_expiry_date: Optional[datetime] = None
    r"""The date the current access token expires in. This field might be overridden by the connector based on the token refresh endpoint response."""

    SOURCE_TYPE: Annotated[
        Annotated[
            SigmaComputing,
            AfterValidator(validate_const(SigmaComputing.SIGMA_COMPUTING)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = SigmaComputing.SIGMA_COMPUTING

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["oauth_access_token", "oauth_token_expiry_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceSigmaComputing.model_rebuild()
except NameError:
    pass
