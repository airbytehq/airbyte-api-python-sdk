"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Netsuite(str, Enum):
    NETSUITE = "netsuite"


class SourceNetsuiteTypedDict(TypedDict):
    consumer_key: str
    r"""Consumer key associated with your integration"""
    consumer_secret: str
    r"""Consumer secret associated with your integration"""
    realm: str
    r"""Netsuite realm e.g. 2344535, as for `production` or 2344535_SB1, as for the `sandbox`"""
    start_datetime: str
    r"""Starting point for your data replication, in format of \"YYYY-MM-DDTHH:mm:ssZ\" """
    token_key: str
    r"""Access token key"""
    token_secret: str
    r"""Access token secret"""
    object_types: NotRequired[List[str]]
    r"""The API names of the Netsuite objects you want to sync. Setting this speeds up the connection setup process by limiting the number of schemas that need to be retrieved from Netsuite."""
    source_type: Netsuite
    window_in_days: NotRequired[int]
    r"""The amount of days used to query the data with date chunks. Set smaller value, if you have lots of data."""


class SourceNetsuite(BaseModel):
    consumer_key: str
    r"""Consumer key associated with your integration"""

    consumer_secret: str
    r"""Consumer secret associated with your integration"""

    realm: str
    r"""Netsuite realm e.g. 2344535, as for `production` or 2344535_SB1, as for the `sandbox`"""

    start_datetime: str
    r"""Starting point for your data replication, in format of \"YYYY-MM-DDTHH:mm:ssZ\" """

    token_key: str
    r"""Access token key"""

    token_secret: str
    r"""Access token secret"""

    object_types: Optional[List[str]] = None
    r"""The API names of the Netsuite objects you want to sync. Setting this speeds up the connection setup process by limiting the number of schemas that need to be retrieved from Netsuite."""

    SOURCE_TYPE: Annotated[
        Annotated[Netsuite, AfterValidator(validate_const(Netsuite.NETSUITE))],
        pydantic.Field(alias="sourceType"),
    ] = Netsuite.NETSUITE

    window_in_days: Optional[int] = 30
    r"""The amount of days used to query the data with date chunks. Set smaller value, if you have lots of data."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["object_types", "window_in_days"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceNetsuite.model_rebuild()
except NameError:
    pass
