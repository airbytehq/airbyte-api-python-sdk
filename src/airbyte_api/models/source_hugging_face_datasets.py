"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class HuggingFaceDatasets(str, Enum):
    HUGGING_FACE_DATASETS = "hugging-face-datasets"


class SourceHuggingFaceDatasetsTypedDict(TypedDict):
    dataset_name: str
    dataset_splits: NotRequired[List[Any]]
    r"""Splits to import. Will import all of them if nothing is provided (see https://huggingface.co/docs/dataset-viewer/en/configs_and_splits for more details)"""
    dataset_subsets: NotRequired[List[Any]]
    r"""Dataset Subsets to import. Will import all of them if nothing is provided (see https://huggingface.co/docs/dataset-viewer/en/configs_and_splits for more details)"""
    source_type: HuggingFaceDatasets


class SourceHuggingFaceDatasets(BaseModel):
    dataset_name: str

    dataset_splits: Optional[List[Any]] = None
    r"""Splits to import. Will import all of them if nothing is provided (see https://huggingface.co/docs/dataset-viewer/en/configs_and_splits for more details)"""

    dataset_subsets: Optional[List[Any]] = None
    r"""Dataset Subsets to import. Will import all of them if nothing is provided (see https://huggingface.co/docs/dataset-viewer/en/configs_and_splits for more details)"""

    SOURCE_TYPE: Annotated[
        Annotated[
            HuggingFaceDatasets,
            AfterValidator(validate_const(HuggingFaceDatasets.HUGGING_FACE_DATASETS)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = HuggingFaceDatasets.HUGGING_FACE_DATASETS

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["dataset_splits", "dataset_subsets"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceHuggingFaceDatasets.model_rebuild()
except NameError:
    pass
