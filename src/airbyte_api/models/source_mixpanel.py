"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class OptionTitleProjectSecret(str, Enum):
    PROJECT_SECRET = "Project Secret"


class ProjectSecretTypedDict(TypedDict):
    api_secret: str
    r"""Mixpanel project secret. See the <a href=\"https://developer.mixpanel.com/reference/project-secret#managing-a-projects-secret\">docs</a> for more information on how to obtain this."""
    option_title: OptionTitleProjectSecret


class ProjectSecret(BaseModel):
    api_secret: str
    r"""Mixpanel project secret. See the <a href=\"https://developer.mixpanel.com/reference/project-secret#managing-a-projects-secret\">docs</a> for more information on how to obtain this."""

    OPTION_TITLE: Annotated[
        Annotated[
            Optional[OptionTitleProjectSecret],
            AfterValidator(validate_const(OptionTitleProjectSecret.PROJECT_SECRET)),
        ],
        pydantic.Field(alias="option_title"),
    ] = OptionTitleProjectSecret.PROJECT_SECRET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["option_title"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class OptionTitleServiceAccount(str, Enum):
    SERVICE_ACCOUNT = "Service Account"


class ServiceAccountTypedDict(TypedDict):
    project_id: int
    r"""Your project ID number. See the <a href=\"https://help.mixpanel.com/hc/en-us/articles/115004490503-Project-Settings#project-id\">docs</a> for more information on how to obtain this."""
    secret: str
    r"""Mixpanel Service Account Secret. See the <a href=\"https://developer.mixpanel.com/reference/service-accounts\">docs</a> for more information on how to obtain this."""
    username: str
    r"""Mixpanel Service Account Username. See the <a href=\"https://developer.mixpanel.com/reference/service-accounts\">docs</a> for more information on how to obtain this."""
    option_title: OptionTitleServiceAccount


class ServiceAccount(BaseModel):
    project_id: int
    r"""Your project ID number. See the <a href=\"https://help.mixpanel.com/hc/en-us/articles/115004490503-Project-Settings#project-id\">docs</a> for more information on how to obtain this."""

    secret: str
    r"""Mixpanel Service Account Secret. See the <a href=\"https://developer.mixpanel.com/reference/service-accounts\">docs</a> for more information on how to obtain this."""

    username: str
    r"""Mixpanel Service Account Username. See the <a href=\"https://developer.mixpanel.com/reference/service-accounts\">docs</a> for more information on how to obtain this."""

    OPTION_TITLE: Annotated[
        Annotated[
            Optional[OptionTitleServiceAccount],
            AfterValidator(validate_const(OptionTitleServiceAccount.SERVICE_ACCOUNT)),
        ],
        pydantic.Field(alias="option_title"),
    ] = OptionTitleServiceAccount.SERVICE_ACCOUNT

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["option_title"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


AuthenticationWildcardTypedDict = TypeAliasType(
    "AuthenticationWildcardTypedDict",
    Union[ProjectSecretTypedDict, ServiceAccountTypedDict],
)
r"""Choose how to authenticate to Mixpanel"""


AuthenticationWildcard = TypeAliasType(
    "AuthenticationWildcard", Union[ProjectSecret, ServiceAccount]
)
r"""Choose how to authenticate to Mixpanel"""


class SourceMixpanelRegion(str, Enum):
    r"""The region of mixpanel domain instance either US or EU."""

    US = "US"
    EU = "EU"


class Mixpanel(str, Enum):
    MIXPANEL = "mixpanel"


class SourceMixpanelTypedDict(TypedDict):
    credentials: AuthenticationWildcardTypedDict
    r"""Choose how to authenticate to Mixpanel"""
    attribution_window: NotRequired[int]
    r"""A period of time for attributing results to ads and the lookback period after those actions occur during which ad results are counted. Default attribution window is 5 days. (This value should be non-negative integer)"""
    date_window_size: NotRequired[int]
    r"""Defines window size in days, that used to slice through data. You can reduce it, if amount of data in each window is too big for your environment. (This value should be positive integer)"""
    end_date: NotRequired[datetime]
    r"""The date in the format YYYY-MM-DD. Any data after this date will not be replicated. Left empty to always sync to most recent date"""
    export_lookback_window: NotRequired[int]
    r"""The number of seconds to look back from the last synced timestamp during incremental syncs of the Export stream. This ensures no data is missed due to delays in event recording. Default is 0 seconds. Must be a non-negative integer."""
    num_workers: NotRequired[int]
    r"""The number of worker threads to use for the sync. The performance upper boundary is based on the limit of your Mixpanel pricing plan. More info about the rate limit tiers can be found on Mixpanel's API <a href=\"https://developer.mixpanel.com/reference/raw-event-e xport#api-export-endpoint-rate-limits\">docs</a>."""
    page_size: NotRequired[int]
    r"""The number of records to fetch per request for the engage stream. Default is 1000. If you are experiencing long sync times with this stream, try increasing this value."""
    project_timezone: NotRequired[str]
    r"""Time zone in which integer date times are stored. The project timezone may be found in the project settings in the <a href=\"https://help.mixpanel.com/hc/en-us/articles/115004547203-Manage-Timezones-for-Projects-in-Mixpanel\">Mixpanel console</a>."""
    region: NotRequired[SourceMixpanelRegion]
    r"""The region of mixpanel domain instance either US or EU."""
    select_properties_by_default: NotRequired[bool]
    r"""Setting this config parameter to TRUE ensures that new properties on events and engage records are captured. Otherwise new properties will be ignored."""
    source_type: Mixpanel
    start_date: NotRequired[datetime]
    r"""The date in the format YYYY-MM-DD. Any data before this date will not be replicated. If this option is not set, the connector will replicate data from up to one year ago by default."""


class SourceMixpanel(BaseModel):
    credentials: AuthenticationWildcard
    r"""Choose how to authenticate to Mixpanel"""

    attribution_window: Optional[int] = 5
    r"""A period of time for attributing results to ads and the lookback period after those actions occur during which ad results are counted. Default attribution window is 5 days. (This value should be non-negative integer)"""

    date_window_size: Optional[int] = 30
    r"""Defines window size in days, that used to slice through data. You can reduce it, if amount of data in each window is too big for your environment. (This value should be positive integer)"""

    end_date: Optional[datetime] = None
    r"""The date in the format YYYY-MM-DD. Any data after this date will not be replicated. Left empty to always sync to most recent date"""

    export_lookback_window: Optional[int] = 0
    r"""The number of seconds to look back from the last synced timestamp during incremental syncs of the Export stream. This ensures no data is missed due to delays in event recording. Default is 0 seconds. Must be a non-negative integer."""

    num_workers: Optional[int] = 3
    r"""The number of worker threads to use for the sync. The performance upper boundary is based on the limit of your Mixpanel pricing plan. More info about the rate limit tiers can be found on Mixpanel's API <a href=\"https://developer.mixpanel.com/reference/raw-event-e xport#api-export-endpoint-rate-limits\">docs</a>."""

    page_size: Optional[int] = 1000
    r"""The number of records to fetch per request for the engage stream. Default is 1000. If you are experiencing long sync times with this stream, try increasing this value."""

    project_timezone: Optional[str] = "US/Pacific"
    r"""Time zone in which integer date times are stored. The project timezone may be found in the project settings in the <a href=\"https://help.mixpanel.com/hc/en-us/articles/115004547203-Manage-Timezones-for-Projects-in-Mixpanel\">Mixpanel console</a>."""

    region: Optional[SourceMixpanelRegion] = SourceMixpanelRegion.US
    r"""The region of mixpanel domain instance either US or EU."""

    select_properties_by_default: Optional[bool] = True
    r"""Setting this config parameter to TRUE ensures that new properties on events and engage records are captured. Otherwise new properties will be ignored."""

    SOURCE_TYPE: Annotated[
        Annotated[Mixpanel, AfterValidator(validate_const(Mixpanel.MIXPANEL))],
        pydantic.Field(alias="sourceType"),
    ] = Mixpanel.MIXPANEL

    start_date: Optional[datetime] = None
    r"""The date in the format YYYY-MM-DD. Any data before this date will not be replicated. If this option is not set, the connector will replicate data from up to one year ago by default."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "attribution_window",
                "date_window_size",
                "end_date",
                "export_lookback_window",
                "num_workers",
                "page_size",
                "project_timezone",
                "region",
                "select_properties_by_default",
                "start_date",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    ProjectSecret.model_rebuild()
except NameError:
    pass
try:
    ServiceAccount.model_rebuild()
except NameError:
    pass
try:
    SourceMixpanel.model_rebuild()
except NameError:
    pass
