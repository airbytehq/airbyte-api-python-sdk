"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Sentry(str, Enum):
    SENTRY = "sentry"


class SourceSentryTypedDict(TypedDict):
    auth_token: str
    r"""Log into Sentry and then <a href=\"https://sentry.io/settings/account/api/auth-tokens/\">create authentication tokens</a>.For self-hosted, you can find or create authentication tokens by visiting \"{instance_url_prefix}/settings/account/api/auth-tokens/\" """
    organization: str
    r"""The slug of the organization the groups belong to."""
    project: str
    r"""The name (slug) of the Project you want to sync."""
    discover_fields: NotRequired[List[Any]]
    r"""Fields to retrieve when fetching discover events"""
    hostname: NotRequired[str]
    r"""Host name of Sentry API server.For self-hosted, specify your host name here. Otherwise, leave it empty."""
    source_type: Sentry


class SourceSentry(BaseModel):
    auth_token: str
    r"""Log into Sentry and then <a href=\"https://sentry.io/settings/account/api/auth-tokens/\">create authentication tokens</a>.For self-hosted, you can find or create authentication tokens by visiting \"{instance_url_prefix}/settings/account/api/auth-tokens/\" """

    organization: str
    r"""The slug of the organization the groups belong to."""

    project: str
    r"""The name (slug) of the Project you want to sync."""

    discover_fields: Optional[List[Any]] = None
    r"""Fields to retrieve when fetching discover events"""

    hostname: Optional[str] = "sentry.io"
    r"""Host name of Sentry API server.For self-hosted, specify your host name here. Otherwise, leave it empty."""

    SOURCE_TYPE: Annotated[
        Annotated[Sentry, AfterValidator(validate_const(Sentry.SENTRY))],
        pydantic.Field(alias="sourceType"),
    ] = Sentry.SENTRY

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["discover_fields", "hostname"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceSentry.model_rebuild()
except NameError:
    pass
