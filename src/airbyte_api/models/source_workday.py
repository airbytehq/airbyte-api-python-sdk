"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class SourceWorkdayAuthenticationTypedDict(TypedDict):
    r"""Credentials for connecting to the Workday (RAAS) API."""

    password: str
    username: str


class SourceWorkdayAuthentication(BaseModel):
    r"""Credentials for connecting to the Workday (RAAS) API."""

    password: str

    username: str


class ReportIdsTypedDict(TypedDict):
    report_id: NotRequired[str]


class ReportIds(BaseModel):
    report_id: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["report_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class Workday(str, Enum):
    WORKDAY = "workday"


class SourceWorkdayTypedDict(TypedDict):
    credentials: SourceWorkdayAuthenticationTypedDict
    r"""Credentials for connecting to the Workday (RAAS) API."""
    host: str
    report_ids: List[ReportIdsTypedDict]
    r"""Report IDs can be found by clicking the three dots on the right side of the report > Web Service > View URLs > in JSON url copy everything between Workday tenant/ and ?format=json."""
    tenant_id: str
    num_workers: NotRequired[int]
    r"""The number of worker threads to use for the sync."""
    source_type: Workday


class SourceWorkday(BaseModel):
    credentials: SourceWorkdayAuthentication
    r"""Credentials for connecting to the Workday (RAAS) API."""

    host: str

    report_ids: List[ReportIds]
    r"""Report IDs can be found by clicking the three dots on the right side of the report > Web Service > View URLs > in JSON url copy everything between Workday tenant/ and ?format=json."""

    tenant_id: str

    num_workers: Optional[int] = 10
    r"""The number of worker threads to use for the sync."""

    SOURCE_TYPE: Annotated[
        Annotated[Workday, AfterValidator(validate_const(Workday.WORKDAY))],
        pydantic.Field(alias="sourceType"),
    ] = Workday.WORKDAY

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["num_workers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceWorkday.model_rebuild()
except NameError:
    pass
