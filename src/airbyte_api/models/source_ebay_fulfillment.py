"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class SourceEbayFulfillmentAPIHost(str, Enum):
    HTTPS_API_EBAY_COM = "https://api.ebay.com"
    HTTPS_API_SANDBOX_EBAY_COM = "https://api.sandbox.ebay.com"


class SourceEbayFulfillmentRefreshTokenEndpoint(str, Enum):
    HTTPS_API_EBAY_COM_IDENTITY_V1_OAUTH2_TOKEN = (
        "https://api.ebay.com/identity/v1/oauth2/token"
    )
    HTTPS_API_SANDBOX_EBAY_COM_IDENTITY_V1_OAUTH2_TOKEN = (
        "https://api.sandbox.ebay.com/identity/v1/oauth2/token"
    )


class EbayFulfillment(str, Enum):
    EBAY_FULFILLMENT = "ebay-fulfillment"


class SourceEbayFulfillmentTypedDict(TypedDict):
    password: str
    redirect_uri: str
    refresh_token: str
    start_date: datetime
    username: str
    api_host: NotRequired[SourceEbayFulfillmentAPIHost]
    refresh_token_endpoint: NotRequired[SourceEbayFulfillmentRefreshTokenEndpoint]
    source_type: EbayFulfillment


class SourceEbayFulfillment(BaseModel):
    password: str

    redirect_uri: str

    refresh_token: str

    start_date: datetime

    username: str

    api_host: Optional[SourceEbayFulfillmentAPIHost] = (
        SourceEbayFulfillmentAPIHost.HTTPS_API_EBAY_COM
    )

    refresh_token_endpoint: Optional[SourceEbayFulfillmentRefreshTokenEndpoint] = (
        SourceEbayFulfillmentRefreshTokenEndpoint.HTTPS_API_EBAY_COM_IDENTITY_V1_OAUTH2_TOKEN
    )

    SOURCE_TYPE: Annotated[
        Annotated[
            EbayFulfillment,
            AfterValidator(validate_const(EbayFulfillment.EBAY_FULFILLMENT)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = EbayFulfillment.EBAY_FULFILLMENT

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["api_host", "refresh_token_endpoint"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceEbayFulfillment.model_rebuild()
except NameError:
    pass
