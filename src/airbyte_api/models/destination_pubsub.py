"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class Pubsub(str, Enum):
    PUBSUB = "pubsub"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'destinationType' key instead.."
)
class DestinationPubsubTypedDict(TypedDict):
    credentials_json: str
    r"""The contents of the JSON service account key. Check out the <a href=\"https://docs.airbyte.com/integrations/destinations/pubsub\">docs</a> if you need help generating this key."""
    project_id: str
    r"""The GCP project ID for the project containing the target PubSub."""
    topic_id: str
    r"""The PubSub topic ID in the given GCP project ID."""
    batching_delay_threshold: NotRequired[int]
    r"""Number of ms before the buffer is flushed"""
    batching_element_count_threshold: NotRequired[int]
    r"""Number of messages before the buffer is flushed"""
    batching_enabled: NotRequired[bool]
    r"""If TRUE messages will be buffered instead of sending them one by one"""
    batching_request_bytes_threshold: NotRequired[int]
    r"""Number of bytes before the buffer is flushed"""
    destination_type: Pubsub
    ordering_enabled: NotRequired[bool]
    r"""If TRUE PubSub publisher will have <a href=\"https://cloud.google.com/pubsub/docs/ordering\">message ordering</a> enabled. Every message will have an ordering key of stream"""


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'destinationType' key instead.."
)
class DestinationPubsub(BaseModel):
    credentials_json: str
    r"""The contents of the JSON service account key. Check out the <a href=\"https://docs.airbyte.com/integrations/destinations/pubsub\">docs</a> if you need help generating this key."""

    project_id: str
    r"""The GCP project ID for the project containing the target PubSub."""

    topic_id: str
    r"""The PubSub topic ID in the given GCP project ID."""

    batching_delay_threshold: Optional[int] = 1
    r"""Number of ms before the buffer is flushed"""

    batching_element_count_threshold: Optional[int] = 1
    r"""Number of messages before the buffer is flushed"""

    batching_enabled: Optional[bool] = False
    r"""If TRUE messages will be buffered instead of sending them one by one"""

    batching_request_bytes_threshold: Optional[int] = 1
    r"""Number of bytes before the buffer is flushed"""

    DESTINATION_TYPE: Annotated[
        Annotated[Pubsub, AfterValidator(validate_const(Pubsub.PUBSUB))],
        pydantic.Field(alias="destinationType"),
    ] = Pubsub.PUBSUB

    ordering_enabled: Optional[bool] = False
    r"""If TRUE PubSub publisher will have <a href=\"https://cloud.google.com/pubsub/docs/ordering\">message ordering</a> enabled. Every message will have an ordering key of stream"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "batching_delay_threshold",
                "batching_element_count_threshold",
                "batching_enabled",
                "batching_request_bytes_threshold",
                "ordering_enabled",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    DestinationPubsub.model_rebuild()
except NameError:
    pass
