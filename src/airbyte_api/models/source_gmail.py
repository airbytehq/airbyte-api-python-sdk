"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Gmail(str, Enum):
    GMAIL = "gmail"


class SourceGmailTypedDict(TypedDict):
    client_id: str
    client_refresh_token: str
    client_secret: str
    include_spam_and_trash: NotRequired[bool]
    r"""Include drafts/messages from SPAM and TRASH in the results. Defaults to false."""
    source_type: Gmail


class SourceGmail(BaseModel):
    client_id: str

    client_refresh_token: str

    client_secret: str

    include_spam_and_trash: Optional[bool] = False
    r"""Include drafts/messages from SPAM and TRASH in the results. Defaults to false."""

    SOURCE_TYPE: Annotated[
        Annotated[Gmail, AfterValidator(validate_const(Gmail.GMAIL))],
        pydantic.Field(alias="sourceType"),
    ] = Gmail.GMAIL

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["include_spam_and_trash"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceGmail.model_rebuild()
except NameError:
    pass
