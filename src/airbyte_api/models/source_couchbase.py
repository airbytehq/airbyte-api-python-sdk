"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Couchbase(str, Enum):
    COUCHBASE = "couchbase"


class SourceCouchbaseTypedDict(TypedDict):
    bucket: str
    r"""The name of the bucket to sync data from"""
    connection_string: str
    r"""The connection string for the Couchbase server (e.g., couchbase://localhost or couchbases://example.com)"""
    password: str
    r"""The password to use for authentication"""
    username: str
    r"""The username to use for authentication"""
    source_type: Couchbase
    start_date: NotRequired[datetime]
    r"""The date from which you'd like to replicate data for incremental streams, in the format YYYY-MM-DDT00:00:00Z. All data generated after this date will be replicated. If not set, all data will be replicated."""


class SourceCouchbase(BaseModel):
    bucket: str
    r"""The name of the bucket to sync data from"""

    connection_string: str
    r"""The connection string for the Couchbase server (e.g., couchbase://localhost or couchbases://example.com)"""

    password: str
    r"""The password to use for authentication"""

    username: str
    r"""The username to use for authentication"""

    SOURCE_TYPE: Annotated[
        Annotated[Couchbase, AfterValidator(validate_const(Couchbase.COUCHBASE))],
        pydantic.Field(alias="sourceType"),
    ] = Couchbase.COUCHBASE

    start_date: Optional[datetime] = None
    r"""The date from which you'd like to replicate data for incremental streams, in the format YYYY-MM-DDT00:00:00Z. All data generated after this date will be replicated. If not set, all data will be replicated."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceCouchbase.model_rebuild()
except NameError:
    pass
