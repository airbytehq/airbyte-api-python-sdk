"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Airbyte(str, Enum):
    AIRBYTE = "airbyte"


class SourceAirbyteTypedDict(TypedDict):
    client_id: str
    client_secret: str
    start_date: datetime
    host: NotRequired[str]
    r"""The Host URL of your Self-Managed Deployment (e.x. airbtye.mydomain.com)"""
    source_type: Airbyte


class SourceAirbyte(BaseModel):
    client_id: str

    client_secret: str

    start_date: datetime

    host: Optional[str] = None
    r"""The Host URL of your Self-Managed Deployment (e.x. airbtye.mydomain.com)"""

    SOURCE_TYPE: Annotated[
        Annotated[Airbyte, AfterValidator(validate_const(Airbyte.AIRBYTE))],
        pydantic.Field(alias="sourceType"),
    ] = Airbyte.AIRBYTE

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["host"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceAirbyte.model_rebuild()
except NameError:
    pass
