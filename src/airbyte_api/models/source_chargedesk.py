"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Chargedesk(str, Enum):
    CHARGEDESK = "chargedesk"


class SourceChargedeskTypedDict(TypedDict):
    username: str
    password: NotRequired[str]
    source_type: Chargedesk
    start_date: NotRequired[int]
    r"""Date from when the sync should start in epoch Unix timestamp"""


class SourceChargedesk(BaseModel):
    username: str

    password: Optional[str] = None

    SOURCE_TYPE: Annotated[
        Annotated[Chargedesk, AfterValidator(validate_const(Chargedesk.CHARGEDESK))],
        pydantic.Field(alias="sourceType"),
    ] = Chargedesk.CHARGEDESK

    start_date: Optional[int] = None
    r"""Date from when the sync should start in epoch Unix timestamp"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["password", "start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceChargedesk.model_rebuild()
except NameError:
    pass
