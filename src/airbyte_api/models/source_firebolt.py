"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated


class SourceFireboltFirebolt(str, Enum):
    FIREBOLT = "firebolt"


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceFireboltTypedDict(TypedDict):
    account: str
    r"""Firebolt account to login."""
    client_id: str
    r"""Firebolt service account ID."""
    client_secret: str
    r"""Firebolt secret, corresponding to the service account ID."""
    database: str
    r"""The database to connect to."""
    engine: str
    r"""Engine name to connect to."""
    host: NotRequired[str]
    r"""The host name of your Firebolt database."""
    source_type: SourceFireboltFirebolt


@deprecated(
    "warning: ** DEPRECATED ** - Typed connector configuration models are deprecated and will be removed in v1.1.0. Pass configuration as a plain dict with a required 'sourceType' key instead.."
)
class SourceFirebolt(BaseModel):
    account: str
    r"""Firebolt account to login."""

    client_id: str
    r"""Firebolt service account ID."""

    client_secret: str
    r"""Firebolt secret, corresponding to the service account ID."""

    database: str
    r"""The database to connect to."""

    engine: str
    r"""Engine name to connect to."""

    host: Optional[str] = None
    r"""The host name of your Firebolt database."""

    SOURCE_TYPE: Annotated[
        Annotated[
            SourceFireboltFirebolt,
            AfterValidator(validate_const(SourceFireboltFirebolt.FIREBOLT)),
        ],
        pydantic.Field(alias="sourceType"),
    ] = SourceFireboltFirebolt.FIREBOLT

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["host"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceFirebolt.model_rebuild()
except NameError:
    pass
