"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Deepset(str, Enum):
    DEEPSET = "deepset"


class DestinationDeepsetTypedDict(TypedDict):
    api_key: str
    r"""Your deepset cloud API key"""
    workspace: str
    r"""Name of workspace to which to sync the data."""
    base_url: NotRequired[str]
    r"""URL of deepset Cloud API (e.g. https://api.cloud.deepset.ai, https://api.us.deepset.ai, etc). Defaults to https://api.cloud.deepset.ai."""
    destination_type: Deepset
    retries: NotRequired[float]
    r"""Number of times to retry an action before giving up."""


class DestinationDeepset(BaseModel):
    api_key: str
    r"""Your deepset cloud API key"""

    workspace: str
    r"""Name of workspace to which to sync the data."""

    base_url: Optional[str] = "https://api.cloud.deepset.ai"
    r"""URL of deepset Cloud API (e.g. https://api.cloud.deepset.ai, https://api.us.deepset.ai, etc). Defaults to https://api.cloud.deepset.ai."""

    DESTINATION_TYPE: Annotated[
        Annotated[Deepset, AfterValidator(validate_const(Deepset.DEEPSET))],
        pydantic.Field(alias="destinationType"),
    ] = Deepset.DEEPSET

    retries: Optional[float] = 5
    r"""Number of times to retry an action before giving up."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["base_url", "retries"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    DestinationDeepset.model_rebuild()
except NameError:
    pass
