"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Dixa(str, Enum):
    DIXA = "dixa"


class SourceDixaTypedDict(TypedDict):
    api_token: str
    r"""Dixa API token"""
    start_date: datetime
    r"""The connector pulls records updated from this date onwards."""
    batch_size: NotRequired[int]
    r"""Number of days to batch into one request. Max 31."""
    source_type: Dixa


class SourceDixa(BaseModel):
    api_token: str
    r"""Dixa API token"""

    start_date: datetime
    r"""The connector pulls records updated from this date onwards."""

    batch_size: Optional[int] = 31
    r"""Number of days to batch into one request. Max 31."""

    SOURCE_TYPE: Annotated[
        Annotated[Dixa, AfterValidator(validate_const(Dixa.DIXA))],
        pydantic.Field(alias="sourceType"),
    ] = Dixa.DIXA

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["batch_size"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceDixa.model_rebuild()
except NameError:
    pass
