"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from airbyte_api.types import BaseModel, UNSET_SENTINEL
from airbyte_api.utils import validate_const
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class Front(str, Enum):
    FRONT = "front"


class SourceFrontTypedDict(TypedDict):
    api_key: str
    start_date: datetime
    page_limit: NotRequired[str]
    r"""Page limit for the responses"""
    source_type: Front


class SourceFront(BaseModel):
    api_key: str

    start_date: datetime

    page_limit: Optional[str] = "50"
    r"""Page limit for the responses"""

    SOURCE_TYPE: Annotated[
        Annotated[Front, AfterValidator(validate_const(Front.FRONT))],
        pydantic.Field(alias="sourceType"),
    ] = Front.FRONT

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["page_limit"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SourceFront.model_rebuild()
except NameError:
    pass
