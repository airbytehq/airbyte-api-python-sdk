# SourceFileStorageProviderProvider8Storage

WARNING: Note that the local storage URL available for reading must start with the local mount "/local/" at the moment until we implement more advanced docker mounting options.

## Example Usage

```python
from airbyte_api.models import SourceFileStorageProviderProvider8Storage

value = SourceFileStorageProviderProvider8Storage.LOCAL
```


## Values

| Name    | Value   |
| ------- | ------- |
| `LOCAL` | local   |