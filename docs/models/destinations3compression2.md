# DestinationS3Compression2

Whether the output files should be compressed. If compression is selected, the output filename will have an extra extension (GZIP: ".jsonl.gz").


## Supported Types

### `models.DestinationS3CompressionNoCompression2`

```python
value: models.DestinationS3CompressionNoCompression2 = /* values here */
```

### `models.DestinationS3GZIP2`

```python
value: models.DestinationS3GZIP2 = /* values here */
```

